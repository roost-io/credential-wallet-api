# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /accounts_patch for http method type PATCH in artillery framework

# RoostTestHash=2758affb7e


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "accounts_patch_success.csv"
    fields:
      - "email"
    skipHeader: true
  environments:
    development:
      target: "{{ $processEnvironment.API_HOST }}"
      headers:
        Token: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Test Accounts API"
    flow:
      - put:
          url: "/accounts"
          headers:
            content-type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
        # Give Artillery time to "think" between requests in the scenario
        think: 1
      - get:
          url: "/accounts"
          headers:
            content-type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "email"
        # Give Artillery time to "think" between requests in the scenario
        think: 1
