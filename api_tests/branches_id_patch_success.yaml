# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches/{id}_patch for http method type PATCH in artillery framework

# RoostTestHash=9caaa2ef96


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "branches_id_patch_success.csv"
    fields:
      - "id"
      - "tradeName"
      - "phone"
      - "email"
    skipHeader: true
  headers:
    Token: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - flow:
      - patch:
          url: "/recipients/branches/{{id}}"
          headers:
            Content-Type: "application/json"
          json:
            tradeName: "{{tradeName}}"
            phone: "{{phone}}"
            email: "{{email}}"
          beforeRequest:
            log: "Initiating PATCH request to update Recipient Branch"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: { "Content-Type": "application/json" }
            - hasProperty: "id"
            - hasProperty: "tradeName"
            - hasProperty: "phone"
            - hasProperty: "email"
          afterResponse:
            log: "Successfully updated Recipient Branch"
      - think: 3
