# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches/{id}_patch for http method type PATCH in artillery framework

# RoostTestHash=9caaa2ef96


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "branches_id_patch_success.csv"
    fields:
      - "tradeName"
      - "phone"
      - "email"
    skipHeader: true
  ensure:
  plugins:
    - expect
scenarios:
  - name: "Test API: /recipients/branches/{id}"
    flow:
      # PATCH request to update a Recipient Branch
      - patch:
          url: "/recipients/branches/{{ id }}"
          json:
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 200
            - hasProperty: ["cocBranchNumber", "cocNumber", "createdAt", "deregistrationDate", "eoriNumber", "id", "tradeName", "tradeNames", "updatedAt"]
            - hasHeader: ["api-version"]
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "application/json"
          think: 5
