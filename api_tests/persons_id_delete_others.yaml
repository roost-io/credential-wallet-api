# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /recipients/persons/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=819791060f



# ********RoostGPT********
config:
  target: '{{ $processEnvironment.API_HOST }}'
  phases:
  - duration: 30
    arrivalCount: 20
  plugins:
    expect: {}
  payload:
    path: "persons_id_delete_others.csv"
    fields:
      - "invalidID"
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"

scenarios:
  - name: "Test API for non-successful cases"
    flow:
      #Testing GET request with invalid ID (non existent)
      - get:
          url: "/recipients/persons/{{ invalidID }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: 'error'
            - equals: 'Person not found'
        think: 2

      # Testing DELETE endpoint with unauthorized token
      - delete:
          url: "/recipients/persons/{{ invalidID }}"
          headers:
            Token: "fake-token"
          expect:
            - statusCode: 401
            - hasProperty: 'error'
            - equals: 'Unauthorized'
        think: 2

      # Testing DELETE endpoint with valid token but non-existent id
      - delete:
          url: "/recipients/persons/{{ invalidID }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: 'error'
            - equals: 'Person not found'
        think: 2
      
      # Test post method with invalid payload
      - post:
          url: "/recipients/persons/{{ invalidID }}"
          headers:
            Token: '{{ $processEnvironment.AUTH_TOKEN }}'
            Content-Type: 'application/json'
          json:
            invalidKey: 'invalidValue'
          expect:
            - statusCode: 400
            - hasProperty: 'error'
            - equals: 'Invalid request payload input'
        think: 2
