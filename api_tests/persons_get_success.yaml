# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/persons_get for http method type GET in artillery framework

# RoostTestHash=aa0a5d4980


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "persons_get_success.csv"
    fields:
      - "size"
      - "offset"
      - "name"
      - "birth-date"
  plugins:
    expect: {}

scenarios:
  - name: "Test /recipients/persons API endpoint"
    flow:
      - get:
          url: "/recipients/persons?size={{ size }}&offset={{ offset }}&name={{ name }}&birth-date={{ birth-date }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "persons"
            - hasProperty: "persons.id"
            - hasProperty: "persons.initials"
            - hasProperty: "persons.firstName"
            - hasProperty: "persons.middleName"
            - hasProperty: "persons.lastName"
            - hasProperty: "persons.birthDate"
            - hasProperty: "persons.birthPlace"
            - hasProperty: "persons.phone"
            - hasProperty: "persons.email"
            - hasProperty: "persons.postalCode"
            - hasProperty: "persons.houseNumber"
            - hasProperty: "persons.houseNumberPostfix"
            - hasProperty: "persons.countryCode"
            - hasProperty: "persons.createdAt"
            - hasProperty: "persons.updatedAt"
            - hasHeader: "api-version"
            - hasHeader: "page-size"
            - hasHeader: "page-offset"
            - equals: { "api-version": "1.0.0" }
      - think: 2
