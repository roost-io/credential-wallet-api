# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /recipients/persons_get for http method type GET in artillery framework
# RoostTestHash=aa0a5d4980



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./my-processors.js"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
   path: "persons_get_success.csv"
   fields: ["PersonName", "BirthDate"]
   skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Test the GET /recipients/persons API endpoint"
    flow:
      # Making a GET request to fetch persons
      - get:
          url: "/recipients/persons?name={{ PersonName }}&birth-date={{ BirthDate }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - contentType: application/json
            - hasProperty: persons
            - hasProperty: total
            # Asserting properties
            - hasProperty: persons[0].id
            - hasProperty: persons[0].initials
            - hasProperty: persons[0].firstName
            - hasProperty: persons[0].lastName
            - hasProperty: persons[0].birthDate
            - hasProperty: persons[0].phone
            - hasProperty: persons[0].postalCode
            - hasProperty: persons[0].houseNumber
            - hasProperty: persons[0].countryCode
            - hasProperty: persons[0].reference
            - hasProperty: persons[0].createdAt
            - hasProperty: persons[0].updatedAt
            - hasHeader: api-version
            - hasHeader: page-size
            - hasHeader: page-offset
      - log: "Received successful response for GET /recipients/persons"
      - think: 1
