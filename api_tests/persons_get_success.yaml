# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/persons_get for http method type GET in artillery framework

# RoostTestHash=aa0a5d4980


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 5
      name: "Warm up"
  payload:
    path: "persons_get_success.csv"
    fields:
      - "size"
      - "offset"
      - "name"
      - "birth-date"
    skipHeader: true

scenarios:
  - name: "Test Scenario"
    flow:
      # Request to fetch recipients persons
      - get:
          url: "/recipients/persons?size={{ size }}&offset={{ offset }}&name={{ name }}&birth-date={{ birth-date }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "persons"
            - hasHeader: "api-version"
            - headerEquals:
                api-version: '1.0.0'
            - hasHeader: "page-size"
            - hasHeader: "page-offset"
            - hasProperty: ["persons", "everyItem", "hasProperty", "id"]
            - hasProperty: ["persons", "everyItem", "hasProperty", "initials"]
            - hasProperty: ["persons", "everyItem", "hasProperty", "firstName"]
            - hasProperty: ["persons", "everyItem", "hasProperty", "middleName"]
            - hasProperty: ["persons", "everyItem", "hasProperty", "lastName"]
            - hasProperty: ["persons", "everyItem", "hasProperty", "birthDate"]
            - hasProperty: ["persons", "everyItem", "hasProperty", "birthPlace"]
            - hasProperty: ["persons", "everyItem", "hasProperty", "phone"]
            - hasProperty: ["persons", "everyItem", "hasProperty", "email"]
            - hasProperty: ["persons", "everyItem", "hasProperty", "postalCode"]
            - hasProperty: ["persons", "everyItem", "hasProperty", "houseNumber"]
            - hasProperty: ["persons", "everyItem", "hasProperty", "houseNumberPostfix"]
            - hasProperty: ["persons", "everyItem", "hasProperty", "countryCode"]
            - hasProperty: ["persons", "everyItem", "hasProperty", "reference"]
            - hasProperty: ["persons", "everyItem", "hasProperty", "createdAt"]
            - hasProperty: ["persons", "everyItem", "hasProperty", "updatedAt"]
        think: 1
