# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /authorizations_post for http method type POST in artillery framework
# RoostTestHash=ed8c1b7bff



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 1
      arrivalRate: 1
  plugins:
    expect: {}
  payloads:
    - path: "authorizations_post_others.csv"
      fields: ["email"]
      skipHeader: true

scenarios:
  - name: 'Test for non-successful API responses'
    flow:
      - post:
          url: '/authorizations' # Valid URL
          json:
            email: "{{ email }}" 
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400 # Induce failure by creating request that should return Bad Request
            - contentType: json
            - hasProperty: email
      - think: 1
      
      - post:
          url: '/wrong-authorizations' # Invalid URL
          json:
            email: "{{ email }}" 
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404 # Test for not found response
      - think: 1
      
      - post:
          url: '/authorizations' # Auth failure
          json:
            email: "{{ email }}"
          headers:
            Token: "wrong-token"
          expect:
            - statusCode: 403
      - think: 1  

      - post:
          url: '/authorizations' # Handling missing email parameter
          json:
            email: ''
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400 # Expecting bad request as email is missing
            - hasProperty: errors  
      - think: 1  
