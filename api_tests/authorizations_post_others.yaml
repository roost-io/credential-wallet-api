# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /authorizations_post for http method type POST in artillery framework

# RoostTestHash=ed8c1b7bff


---
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "authorizations_post_others.csv"
    fields: ["email"]
    skipHeader: true

scenarios:
  - name: "Testing non-successful API responses"
    flow:
      - comment: "An invalid POST request with missing authorization in header"
        post:
          url: "/authorizations"
          headers:
            Content-Type: "application/json"
          json:
            email: "{{ email }}"
        think: 1
      - comment: "An invalid POST request with incorrect email format in payload"
        post:
          url: "/authorizations"
          headers:
            Content-Type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "not.an.email"
        expect:
          - statusCode: 400
        think: 1
      - comment: "An invalid GET request to non-existent URL"
        get:
          url: "/non/existent/url"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
        think: 1
      - comment: "An invalid GET request with missing authorization in header"
        get:
          url: "/authorizations/497f6eca-6276-4993-bfeb-53cbbbba6f08"
        expect:
          - statusCode: 401
        think: 1
