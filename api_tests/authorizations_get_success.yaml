# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /authorizations_get for http method type GET in artillery framework

# RoostTestHash=3b64f22044


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  headers:
    Authorization: "Token {{$processEnvironment.AUTH_TOKEN}}"
  payload:
    path: "authorizations_get_success.csv"
    fields:
      - id
      - email
      - name
      - createdAt
      - updatedAt
  environments:
    dev:
      target: "" 
scenarios:
  - name: "Authorization Scenario"
    flow:
      - log: "Requesting authorization data"
      - get:
          url: "/authorizations"
          headers:
            api-version: "1.0.0"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
            - hasProperty: ["authorizations", "total"]
      - think: 1
      - log: "Requesting specific authorization detail"
      - get:
          url: "/authorizations/{{ id }}"
          headers:
            api-version: "1.0.0"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
            - hasProperty: ["id", "email", "name", "createdAt", "updatedAt"]
      - think: 1
