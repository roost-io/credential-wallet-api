# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /authorizations_get for http method type GET in artillery framework

# RoostTestHash=3b64f22044


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "authorizations_get_success.csv"
    fields: ["id", "name", "email", "createdAt", "updatedAt"]
    skipHeader: true  
  headers:
    Token: "{{$processEnvironment.AUTH_TOKEN}}"
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - name: Test API authorizations endpoint
    flow:
      - get:
          url: "/authorizations"
  
          # Testing the GET /authorizations operation
          # Expecting a 200 status code, and a JSON object containing required data in the response body
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasHeader: "api-version"
            - headerEquals: ["api-version", "1.0.0"]
            - hasProperty: ["total"]
            - hasProperty: ["authorizations"]
            - equals: ["total", 1]
          
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          
          afterResponse: "SetAuthorization"

      - think: 2

      - post: 
          # Replace id with a value from the payload
          url: "/authorizations/{{ id }}"
            
          # Testing the POST /authorizations/{id} operation
          # Expecting a 200 status code, and the id, name, email, createdAt and updatedAt fields in the response body
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: ["id", "name", "email", "createdAt", "updatedAt"]

          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
