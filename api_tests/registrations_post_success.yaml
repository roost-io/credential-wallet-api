# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches/{id}/registrations_post for http method type POST in artillery framework

# RoostTestHash=16a6858a84


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "registrations_post_success.csv"
    fields:
      - "id"
      - "credentialUrl"
      - "startDate"
      - "endDate"
      - "reference"
    skipHeader: true
  environments:
    prod:
      target: "{{ $processEnvironment.API_HOST }}"

scenarios:
  - name: 'POST /recipients/branches/{id}/registrations'
    flow:
      - log: 'Creating new Registration for the Recipient at Recipient Branch'
      - post:
          url: '/recipients/branches/{{id}}/registrations'
          headers:
            Token: '{{ $processEnvironment.AUTH_TOKEN }}'
            Content-Type: "application/json"
          json:
            reference: "{{ reference }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          expect:
            - statusCode: 201
            - hasProperty: 'id'
            - hasProperty: 'createdAt'
            - hasProperty: 'updatedAt'
            - hasProperty: 'startDate'
            - hasProperty: 'endDate'
            - hasProperty: 'credentialUrl'
            - hasProperty: 'reference'
            - hasProperty: 'status'
            - hasProperty: 'issuedAt'
            - hasProperty: 'suspendedUntil'
            - hasHeader: 'api-version'
            - hasHeader: 'location'
      - think: 1
