# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches/{id}/registrations_post for http method type POST in artillery framework

# RoostTestHash=16a6858a84


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
  - duration: 20
    arrivalRate: 2
  payload:
    path: "registrations_post_success.csv"
    skipHeader: true
    fields:
      - id
      - reference
      - startDate
      - endDate
      - credentialUrl
  plugins:
    expect: {}

scenarios:
- name: "Post new Registrations for Recipient Test"
  weight: 1
  flow:
    - post:
        url: "/recipients/branches/{{ id }}/registrations"
        headers:
          Content-Type: "application/json"
          Token: "{{$processEnvironment.AUTH_TOKEN}}"
        json:
          reference: "{{ reference }}"
          startDate: "{{ startDate }}"
          endDate: "{{ endDate }}"
          credentialUrl: "{{ credentialUrl }}"
        expect:
          - statusCode: 201
          - hasHeader: "api-version"
          - headerEquals: ["location", "/recipients/branches/{{ id }}/registrations"]
          - hasProperty: "id"
          - hasProperty: "reference"
          - hasProperty: "startDate"
          - hasProperty: "endDate"
          - hasProperty: "credentialUrl"
          - hasProperty: "createdAt"
          - hasProperty: "updatedAt"
    - think: 1
