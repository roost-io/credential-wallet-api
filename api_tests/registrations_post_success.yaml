# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/persons/{id}/registrations_post for http method type POST in artillery framework

# RoostTestHash=0973c499e9


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "registrations_post_success.csv"
    skipHeader: true
    fields:
      - "id"
      - "reference"
      - "startDate"
      - "endDate"
      - "credentialUrl"

scenarios:
  - name: "Create new Registration"
    flow:
      - log: "Generating API Authorization Token"
  
      - get:
          url: "/recipients/persons/{{ id }}/registrations"
          headers:
            Accept: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          beforeRequest:
            'log': 'Creating new Registration'
          expect:
            - statusCode: 201
            - hasProperty: "id"
            - hasProperty: "reference"
            - hasProperty: "startDate"
            - hasProperty: "endDate"
            - hasProperty: "status"
            - hasProperty: "credentialUrl"
            - hasProperty: "suspendedUntil"
            - hasProperty: "issuedAt"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
            - headerEquals: {"Content-Type": "application/json"}
            - matchesRegexp: {"credentialUrl": "^\\w+:(\\/?\\/?)[^\\s]+$"}

      - think: 1
