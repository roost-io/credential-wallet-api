# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches/{id}_get for http method type GET in artillery framework

# RoostTestHash=061355b093


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "branches_id_get_success.csv"
    fields:
      - "id"
    skipHeader: true
  headers:
    "Token": "{{$processEnvironment.AUTH_TOKEN}}"
scenarios:
  - name: "Branch API tests"
    flow:
      - log: "GET request to /recipients/branches/{id} API endpoint"
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "cocBranchNumber"
            - hasProperty: "cocNumber"
            - hasProperty: "tradeName"
            - hasProperty: "tradeNames"
            - hasProperty: "eoriNumber"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
            - hasProperty: "deregistrationDate"
            - hasHeader: "api-version"
      - think: 1
      - log: "Completed GET request to /recipients/branches/{id} API endpoint"
