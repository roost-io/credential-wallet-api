# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches_get for http method type GET in artillery framework

# RoostTestHash=74a40da00e


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
      name: "Test invalid APIs"
  plugins:
    expect: {}
  payload:
    path: "./payloads/branches_get_others.csv"
    fields:
      - "coc-number"
      - "coc-branch-number"
      - "trade-name"
      - "size"
      - "offset"
    skipHeader: true
  processor: "./lib/my-functions.js"
scenarios:
- name: "Test scenario for Non-successful API requests"
  flow:
    # Test case : Test with invalid url
    - think: 1
    - get:
        url: "/invalid/url"
        expect:
          - statusCode: 404

    # Test case : Test with missing Authorization token
    - think: 1
    - get:
        url: "/recipients/branches"
        headers:
          Authorization: " "
        expect:
          - statusCode: 403

    # Test case: Test with invalid data size in query param      
    - think: 1
    - get:
        url: "/recipients/branches?size={{ size }}&offset={{ offset }}"
        headers:
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400

    # Test case: Test with invalid coc-number in query param      
    - think: 1
    - get:
        url: "/recipients/branches?coc-number={{ coc-number }}"
        headers:
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
          
    # Test case: Test with invalid query parameters      
    - think: 1
    - get:
        url: "/recipients/branches?invalidparam=invalid"
        headers:
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400

