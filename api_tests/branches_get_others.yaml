# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /recipients/branches_get for http method type GET in artillery framework
# RoostTestHash=74a40da00e



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins: 
    expect: {}
  payload:
    path: "branches_get_others.csv"
    fields:
      - coc-number
      - coc-branch-number
      - trade-name
      - size
      - offset
    skipHeader: true
  defaults:
    headers:
      Token: "{{$processEnvironment.AUTH_TOKEN}}"
scenarios:
  - name: "Unsuccessful API calls"
    flow:
      # Expecting an unsuccessful response due to invalid token
      - think: 1
      - get:
          url: "/recipients/branches?coc-branch-number={{ coc-branch-number }}"
          expect:
            - statusCode: 401

      # Expecting an unsuccessful response due to incorrect URL
      - think: 1
      - get:
          url: "/recipients/branches/invalid-path"
          expect:
            - statusCode: 404

      # Expecting an unsuccessful response due to missing required query parameter
      - think: 1
      - get:
          url: "/recipients/branches?size={{ size }}"
          expect:
            - statusCode: 400

      # Expecting an unsuccessful response due to invalid datatype of a query parameter
      - think: 1 
      - get:
          url: "/recipients/branches?offset=INVALID_DATATYPE"
          expect:
            - statusCode: 400
