# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /recipients/branches/{id}_get for http method type GET in artillery framework
# RoostTestHash=061355b093



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "branches_id_get_others.csv"
    fields: ["id"]
    skipHeader: true        
  environments:
    development:
      target: "{{ $processEnvironment.API_HOST }}"
      phases:
        - duration: 60
          arrivalRate: 20

scenarios:
  - name: "Test scenario for non-successful API requests"
    flow:
      
      # Testing an invalid URL
      - get:
          url: "/recipients/branches/{id}/invalid-url"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
          think: 1

      # Testing invalid header (missing token)
      - get:
          url: "/recipients/branches/{{ id }}"
          think: 1
          expect:
            - statusCode: 401

      # Testing missing path parameter 'id'
      - get:
          url: "/recipients/branches/"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
          think: 1

      # Testing with ID not in UUID format
      - get:
          url: "/recipients/branches/12345"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
          think: 1

      # Sending an unauthorized request
      - get:
          url: "/recipients/branches/{{ id }}/"
          headers:
            Token: "InvalidToken"
          expect:
            - statusCode: 403
          think: 1

      # Testing correct API endpoint but with unavailable ID in UUID format
      - get:
          url: "/recipients/branches/7e57d004-2b97-0e7a-b45f-5387367791cd"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
          think: 1

