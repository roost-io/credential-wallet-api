# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=061355b093

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
  - duration: 60
    arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "branches_id_get_others.csv"
    fields:
    - id
    skipHeader: true
  headers:
    Token: "{{ $processEnvironment.AUTH_TOKEN }}" 

scenarios:
- flow:
  - log: "test: GET request to an invalid URL"
  - get:
      url: "/invalid/url/{{ id }}"
      expect:
        - hasHeader: "api-version"
        - statusCode: 404
  - think: 1

  - log: "test: GET request to correct url but unauthenticated"
  - get:
      url: "/recipients/branches/{{ id }}"
      expect:
        - hasHeader: "api-version"
        - statusCode: 401
  - think: 1

  - log: "test: GET request to invalid UUID in the Path"
  - get:
      url: "/recipients/branches/invalid_uuid"
      expect:
        - hasHeader: "api-version"
        - statusCode: 400
  - think: 1

  - log: "test: GET request correct UUID but unauthenticated"
  - get:
      url: "/recipients/branches/{{ id }}"
      headers:
        Token: "Invalid Token"
      expect:
        - hasHeader: "api-version"
        - statusCode: 401
  - think: 1
