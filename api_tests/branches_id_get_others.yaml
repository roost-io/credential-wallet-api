# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches/{id}_get for http method type GET in artillery framework

# RoostTestHash=061355b093


config:
  target: "{{ $processEnvironment.API_HOST }}"
  # Enabling the 'expect' plugin for response assertions
  plugins:
    expect: {}

  # Declaring payload variables from CSV file
  payload:
    path: "branches_id_get_others.csv"
    fields:
      - "branchId"
    skipHeader: true

scenarios:
  - name: "Testing non-success API requests"

    # Scenario flow for various erroneous requests
    flow:

      # Unauthorized request
      - log: "Unauthorized request - Expects 401"
        get:
          url: "/recipients/branches/{{$payload.branchId}}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 401

        # Pause for a while after this request
        think: 1

      # Request with an invalid API endpoint
      - log: "Request to an invalid URL - Expects 404"
        get:
          url: "/invalid-url"
          headers:
            Content-Type: "application/json"
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404

        # Pause for a while after this request
        think: 1

      # Request with Invalid payload
      - log: "Request with Invalid payload - Expects 400"
        post:
          url: "/recipients/branches/{{$payload.branchId}}"
          headers:
            Content-Type: "application/json"
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            invalidKey: "invalidValue"
          expect:
            - statusCode: 400

        # Pause for a while after this request
        think: 1  
