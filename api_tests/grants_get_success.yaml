# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /grants_get for http method type GET in artillery framework

# RoostTestHash=692c6b853f


config:
  target: "{{ $processEnvironment.API_HOST }}"
  processors: 
    grantApiYml: "./processors/grantApiYml.js"
  plugins:
    expect: {}
  payload:
    path: "grants_get_success.csv"
    fields:
      - "accountEmail"
      - "accountId"
      - "imageName"
      - "name"
  headers:
    Token: "{{$processEnvironment.AUTH_TOKEN}}"
scenarios:
  - name: "Successful API request scenario for Grants data"
    flow:
      - get:
          url: "/grants"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          capture:
            - header: "api-version"
              as: "apiVer"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - contentType: "application/json"
            - hasProperty: "total"
            - hasProperty: "grants"
            - hasProperty: "grants[0].id"
            - matchesRegexp:
              - "grants[0].id" 
              - "\\{8}-\\{4}-\\{4}-\\{4}-\\{12}"
            - hasProperty: "grants[0].token"
            - hasProperty: "grants[0].account"
            - hasHeader: "Token"
            - equals: ["headers.Token", "{{$processEnvironment.AUTH_TOKEN}}"]
            - hasProperty: "account.id"
            - hasProperty: "account.email"
      - think: 1
