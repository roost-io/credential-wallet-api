# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /grants_get for http method type GET in artillery framework

# RoostTestHash=692c6b853f


config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    maxErrorRate: 0
  phases:
    - duration: 120
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: grants_get_success.csv
    fields:
      - email
  http:
    pool: 5
    timeout: 20
    headers:
      Token : "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: 'Grants API Scenario'
    flow:
      # Request 1: GET /grants
      - log: "GET /grants Request"
      - get:
          url: "/grants"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            json: "$.grants"
            as: "grants"
          expect:
            - statusCode: 200 # successful operation
            - contentType: "application/json" # expecting JSON response
            - hasProperty: "total"
            - hasProperty: "grants[].id"
            - hasProperty: "grants[].token"
            - hasProperty: "grants[].createdAt"
            - hasProperty: "grants[].updatedAt"
            - hasProperty: "grants[].account"
            - hasProperty: "grants[].account.id"
            - hasProperty: "grants[].account.name"
            - hasProperty: "grants[].account.image"
            - hasProperty: "grants[].account.email"
        think: 10
