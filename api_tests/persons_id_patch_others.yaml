# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/persons/{id}_patch for http method type PATCH in artillery framework

# RoostTestHash=1416aa2295


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "persons_id_patch_others.csv"
    skipHeader: true
    fields:
      - "id"
      - "initials"
      - "firstName"
      - "middleName"
      - "lastName"
      - "birthDate"
      - "birthPlace"
      - "phone"
      - "email"
      - "postalCode"
      - "houseNumber"
      - "houseNumberPostfix"
      - "countryCode"
      - "reference"
  plugins:
    expect: {}
scenarios:
  - name: "Testing failed cases of API"
    flow:
      - post:
          url: "/recipients/persons/{{ id }}"
          headers:
            Token: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json: 
            id: "{{ id }}"
            initials: "{{ initials }}"
            firstName: "{{ firstName }}"
            middleName: "{{ middleName }}"
            lastName: "{{ lastName }}"
            birthDate: "{{ birthDate }}"
            birthPlace: "{{ birthPlace }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
            postalCode: "{{ postalCode }}"
            houseNumber: "{{ houseNumber }}"
            houseNumberPostfix: "{{ houseNumberPostfix }}"
            countryCode: "{{ countryCode }}"
            reference: "{{ reference }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
        think: 1
      - get:
          url: "/recipients/persons/abcd-efgh-ijkl"
          headers:
            Token: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
        think: 1
      - patch:
          url: "/recipients/persons/{{ id }}"
          headers:
            Token: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
        think: 1
