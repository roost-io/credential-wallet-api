# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches/{id}_patch for http method type PATCH in artillery framework

# RoostTestHash=9caaa2ef96


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "branches_id_patch_others.csv"
    fields:
      - "id"
      - "tradeName"
      - "phone"
      - "email"
    skipHeader: true
  defaults:
    headers:
      Token: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Test non-success API responses"
    flow:
      # Making a request with invalid ID
      - post:
          url: "/recipients/branches/{{ id }}"
          json:
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 404
          think: 1

      # Making a request with valid ID but invalid payload
      - post:
          url: "/recipients/branches/{{ id }}"
          json:
            tradeName: "InvalidTradeName"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 400
          think: 1

      # Making a request with valid ID and payload but without auth headers
      - post:
          url: "/recipients/branches/{{ id }}"
          headers:
            Token: "" # No auth token provided
          json:
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 401  # Expecting Unauthorized error
          think: 1 

      # Making a request to a non-existing endpoint
      - post:
          url: "/recipients/invalid/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 404  # Expecting NotFound error
          think: 1
