# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=9caaa2ef96

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    fields:
      - column1
      - column2
    skipHeader: true
    path: "data/branches_id_patch_others.csv"
  headers:
    Token: "{{$processEnvironment.AUTH_TOKEN}}"
  
scenarios:
  - name: "Test non-success cases"
    flow:
      - get:
          url: "/invalid_endpoint"
          # Expecting 404 (not found) for invalid URL
          expect:
            - statusCode: 404
          # 5 seconds waiting time before next request
          think: 5
      
      - patch: 
          url: "/recipients/branches/{{ column1 }} "
          json:
            tradeName: "{{ column2 }}"
            email: "invalid email"
          # Expecting 400 for invalid email pattern
          expect:
            - statusCode: 400
          # 5 seconds waiting time before next request
          think: 5
      
      - post:
          url: "/recipients/branches/{{ column1 }}"
          json:
            tradeName: "{{ column2 }}"
            phone: ""
          # Expecting 400 for empty phone number
          expect:
            - statusCode: 400
          # 5 seconds waiting time before next request
          think: 5

      - delete:
          url: "/recipients/branches/{{ column1 }}"
          headers:
            Token: "invalid_auth_token"
          expect:
            # Expecting 401 for unauthorized access
            - statusCode: 401
          # 5 seconds waiting time before next request
          think: 5
      
