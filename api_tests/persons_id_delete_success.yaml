# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/persons/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=819791060f


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "persons_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "Successful API requests"
    flow:
      - get:
          url: "/recipients/persons/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: 'api-version'
            - headerEquals: ['api-version', '1.0.0']
        # Expected Outcome: Successful GET request, retrieving recipient person data
        think: 1

      - post: 
          url: "/recipients/persons/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 201
        # Expected Outcome: Successfully created new recipient person data
        think: 1

      - put: 
          url: "/recipients/persons/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
        # Expected Outcome: Successfully updated recipient person data
        think: 1

      - delete:
          url: "/recipients/persons/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
        # Expected Outcome: Successfully deleted recipient person data
        think: 1
      
