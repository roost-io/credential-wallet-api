# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /recipients/persons/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=819791060f



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "persons_id_delete_success.csv"
    fields: ["id"]
    skipHeader: true
  plugins:
    expect: {}
      
scenarios:
  - name: Test API responses for non-success status codes
    flow:
      - log: "Get request - expects non-success status codes"
        get:
          url: "/recipients/persons/{{ id }}"
          headers:
            Authorization: "Token {{ $processEnvironment.AUTH_TOKEN }}"
          expect: 
            - hasHeader: api-version
            - equals: 
                - "{{ statusCode }}"
                - 200

      - think: 5  
      
      - log: "Post request - expects non-success status codes"
        post:
          url: "/recipients/persons/{{ id }}"
          headers:
            Authorization: "Token {{ $processEnvironment.AUTH_TOKEN }}"
          expect: 
            - statusCode: 201

      - think: 5 

      - log: "Delete request - expects non-success status codes"
        delete:
          url: "/recipients/persons/{{ id }}"
          headers:
            Authorization: "Token {{ $processEnvironment.AUTH_TOKEN }}"
          expect: 
            - statusCode: 204

      - think: 5  

      - log: "Patch request - expects non-success status codes"
        patch:
          url: "/recipients/persons/{{ id }}"
          headers:
            Authorization: "Token {{ $processEnvironment.AUTH_TOKEN }}"
          expect: 
            - statusCode: 200
      - think: 5        
