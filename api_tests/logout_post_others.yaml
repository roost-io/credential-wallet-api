# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /logout_post for http method type POST in artillery framework

# RoostTestHash=64d33906e5


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "logout_post_others.csv"
    fields:
      - "field1"
      - "field2"
    skipHeader: true
scenarios:
  - name: API failure cases
    flow:
      # Unauthorized
      - post:
          url: "/logout"
          headers:
            Token: "fake_token_123"
          expect:
            - statusCode: 401
        think: 5
      # Correct token but incorrect request method
      - patch:
          url: "/logout"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
        think: 5
      # Valid request with incorrect payload
      - post:
          url: "/logout"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            field1: "{{ field1 }}"
            field2: "{{ invalid_field2 }}"
          expect:
            - statusCode: 400
        think: 5
      # Invalid URL
      - post:
          url: "/log_out"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
          expect:
            - statusCode: 404
        think: 5
