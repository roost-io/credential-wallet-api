# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /logout_post for http method type POST in artillery framework

# RoostTestHash=64d33906e5


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 20
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "logout_post_others.csv"
    fields: ["Token"]
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"

scenarios:
  - name: "Test non-successful responses"
    flow:
      - post:
          url: "/exampleurl"
          headers: 
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect: 
            - statusCode: 404
        # This request attempts an API endpoint that doesn't exist, expecting a 404 response
        think: 1

      - get:
          url: "/logout"
          headers: 
            Token: "wrongtoken"
          expect: 
            - statusCode: 401
        # This request attempts to use an invalid authentication token, expecting a 401 response
        think: 1

      - post:
          url: "/logout"
          json: 
            invalidPayload: "data"
          headers: 
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect: 
            - statusCode: 400
        # This request includes an incorrect (invalid) payload, expecting a 400 response
        think: 1

      - put:
          url: "/logout"
          json: 
            invalidPayload: "data"
          headers: 
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect: 
            - statusCode: 405
        # This request attempts a method not allowed by the API, expecting a 405 response
        think: 1

      - get:
          url: "/logout"
          headers: 
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect: 
            - statusCode: 200
            - hasProperty: "error"
        # This request tests a successful request for a non-successful outcome (error in the response), expecting a 200 response with error in the body.
        think: 1
