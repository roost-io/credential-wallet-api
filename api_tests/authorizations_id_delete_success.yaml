# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=cb128291e8


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "authorizations_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true
  phases:
    - duration: 180
      arrivalRate: 20
scenarios:
  - flow:
      # First API request
      - get:
          url: "/authorizations/{{ id }}"
          # Ensure we only allow successful requests, expecting a 200 status code
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          think: 1
      # Second API request
      - delete:
          url: "/authorizations/{{ id }}"
          # Expect a successful delete operation
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          think: 1
