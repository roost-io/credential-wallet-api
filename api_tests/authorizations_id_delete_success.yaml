# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=cb128291e8


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "authorizations_id_delete_success.csv"
    fields: ["id"]
    skipHeader: true
  headers:
    Token: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Remove Authorized Account Test"
    flow:
      # Get authorization request
      - get:
          url: "/authorizations/{{ id }}"
          # Request headers
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            json: "$"
            as: "response"
          expect:
            - statusCode: 200
            # Checking API version
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
      - think: 2
      # Delete authorization request
      - delete:
          url: "/authorizations/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            # Expecting API version
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
      - think: 2
