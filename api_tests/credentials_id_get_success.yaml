# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /credentials/{id}_get for http method type GET in artillery framework

# RoostTestHash=c02d3561ab


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "credentials_id_get_success.csv"
    fields: 
      - "id"
  plugins:
    expect: {}

scenarios:
  - name: Get API /credentials/{id} scenario
    flow:
      - get:
          url: "/credentials/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            # Expecting status code to be 200
            - statusCode: 200
            # Expecting resulting JSON to have specific fields
            - hasProperty: 'id'
            - hasProperty: 'format'
            - hasProperty: 'credential'
            - hasProperty: 'status'
            - hasProperty: 'statusUpdatedAt'
            - hasProperty: 'createdAt'
            #Expecting api-version in header
            - hasHeader: 'api-version'
      - think: 2
