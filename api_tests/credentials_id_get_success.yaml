# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /credentials/{id}_get for http method type GET in artillery framework
# RoostTestHash=c02d3561ab



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "credentials_id_get_success.csv"
    fields: ["id"] 
    skipHeader: true 

scenarios:
  - name: "Test suite for Non-Success API Responses"
    flow:
      
      - comment: "Test GET credentials/id with successful response code. Here ID is uuid."
        get:
          url: "/credentials/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "createdAt"
            - hasProperty: "credential"
            - hasProperty: "format"
            - hasProperty: "id"
            - hasProperty: "status"
            - hasProperty: "statusUpdatedAt"
            - hasHeader: "api-version"
            - contentType: "application/json"
        think: 5
