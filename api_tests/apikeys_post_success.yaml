# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /apikeys_post for http method type POST in artillery framework

# RoostTestHash=28a54f1579


config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    p95: 200
  phases:
    - duration: 20
      arrivalRate: 5
  payload:
    path: "apikeys_post_success.csv"
    fields:
      - "id"
      - "token"
  plugins:
    expect: {}
    
scenarios:
  - name: "API keys test"
    flow:
      - post:
          url: "/apikeys"
          headers:
            {{ $processEnvironment.AUTH_TOKEN }}: "Token"
          expect:
            - statusCode: 201
          # Scenario - Create API Key (Create Session with type = 4)
          # Request - POST /apikeys
          # Expected Outcome - Successful operation

      - think: 1

      - get:
          url: "/apikeys"
          headers:
            {{ $processEnvironment.AUTH_TOKEN }}: "Token"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "token"
            - hasHeader: "api-version"
            - hasHeader: "location"
          # Scenario - Retrieve all API keys
          # Request - GET /apikeys
          # Expected Outcome - Successful operation with returned list of API keys

      - think: 1
