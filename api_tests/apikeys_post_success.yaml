# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /apikeys_post for http method type POST in artillery framework

# RoostTestHash=28a54f1579


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  variables:
    authToken: "{{ $processEnvironment.AUTH_TOKEN }}"
  schemas:
    ApiKeyAuth: { type: "apiKey", name: "Token", in: "header" }
  plugins:
    expect: {}
  payload:
    path: "apikeys_post_success.csv"
    fields: ["token", "createdAt", "id"] 
    skipHeader: true
  processor: "./processor.js"

scenarios:
  - name: "Post API Key Scenario"
    flow:
      - post:
          url: "/apikeys"
          headers:
            Token: "{{ authToken }}"
            Content-Type: "application/json"
          expect:
            - statusCode: 201
            - hasHeader: "api-version"
            - headerEquals: { "location": "/apikeys/{{ id }}" }
            - hasProperty: ["id", "createdAt", "token"]
          capture:
            - json: "$.id"
              as: "id"
          think: 1
