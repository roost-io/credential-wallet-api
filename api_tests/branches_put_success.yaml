# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches_put for http method type PUT in artillery framework

# RoostTestHash=63322ffb26


config:
  target: "{{ $processEnvironment.API_HOST }}"
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "branches_put_success.csv"
    fields:
      - "cocNumber"
      - "cocBranchNumber"
      - "city"
      - "tradeName"
      - "phone"
      - "email"
  headers:
    token: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "API requests"
    flow:
      - post:
          url: "/recipients/branches"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: '{{ email }}'
          headers:
            api-version: "1.0.0"
          expect:
            - statusCode: 201
              hasHeaders: 
                - "api-version"
                - "location"
              hasProperties: 
                - "tradeName"
                - "cocBranchNumber"
                - "cocNumber"
                - "createdAt"
                - "updatedAt"
          log: "A new branch has been added successfully"
        think: 1
      - get:
          url: "/recipients/branches"
          headers:
            api-version: "1.0.0"
          expect:
            - statusCode: 200
              hasHeaders: 
                - "api-version"
                - "location"
              hasProperties: 
                - "tradeName"
                - "cocBranchNumber"
                - "cocNumber"
                - "createdAt"
                - "updatedAt"
          log: "A list of branches has been retrieved successfully"
        think: 1
      - get:
          url: "/recipients/branches/497f6eca-6276-4993-bfeb-53cbbbba6f08"
          headers:
            api-version: "1.0.0"
          expect:
            - statusCode: 200
              hasHeaders: 
                - "api-version"
                - "location"
              hasProperties: 
                - "tradeName"
                - "cocBranchNumber"
                - "cocNumber"
                - "createdAt"
                - "updatedAt"
          log: "Branch details have been retrieved successfully"
        think: 1
      - get:
          url: "/recipients/branches/497f6eca-6276-4993-bfeb-53cbbbba6f08/registrations"
          headers:
            api-version: "1.0.0"
          expect:
            - statusCode: 200
              hasHeaders: 
                - "api-version"
                - "location"
              hasProperties: 
                - "tradeName"
                - "cocBranchNumber"
                - "cocNumber"
                - "createdAt"
                - "updatedAt"
          log: "Branch registrations have been retrieved successfully"
        think: 1
