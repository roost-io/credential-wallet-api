# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches_put for http method type PUT in artillery framework

# RoostTestHash=63322ffb26


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "branches_put_success.csv"
    fields:
      - "cocNumber"
      - "cocBranchNumber"
      - "city"
      - "tradeName"
      - "phone"
      - "email"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Valid request scenario"
    flow:
      # Post Request - Test  for successful addition of branch
      - post:
          url: "/recipients/branches"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
              hasProperty: ['cocBranchNumber','cocNumber','createdAt','deregistrationDate','eoriNumber','id','tradeName','tradeNames','updatedAt']
            - statusCode: 201
              hasProperty: ['cocBranchNumber','cocNumber','createdAt','deregistrationDate','eoriNumber','id','tradeName','tradeNames','updatedAt']
          capture:
            - json: "$.id"
              as: "branchId"
        think: 1
