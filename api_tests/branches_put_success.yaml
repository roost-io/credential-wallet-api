# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /recipients/branches_put for http method type PUT in artillery framework
# RoostTestHash=63322ffb26



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "branches_put_success.csv"
    fields:
      - "cocNumber"
      - "cocBranchNumber"
      - "city"
      - "tradeName"
      - "phone"
      - "email"
  processors: {}

scenarios:
  - name: "Successful API requests"
    flow:
      - post:
          url: "/recipients/branches"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "email"
            - hasProperty: "phone"
            - hasProperty: "cocNumber"
            - hasProperty: "cocBranchNumber"
            - hasProperty: "tradeName"
            - hasProperty: "tradeNames"
            - hasProperty: "eoriNumber"
            - hasProperty: "deregistrationDate"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
          capture:
            - json: "$.id"
              as: "branchId"
          think: 1
      - log: "Created branch with ID - {{ branchId }}"
      - put:
          url: "/recipients/branches/{{ branchId }}"
          json:
            tradeName: "{{ tradeName }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "tradeName"
          think: 1
      - log: "Updated branch with ID - {{ branchId }}"
      - delete:
          url: "/recipients/branches/{{ branchId }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
          think: 1
      - log: "Deleted branch with ID - {{ branchId }}"
