# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /credentials/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=f1fc1d06f7


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  ensure:
    # Test will fail if p95 latency not below 200ms
    p95: 200
  plugins:
    expect: {}
  payload:
    path: "credentials_id_delete_others.csv"
    fields:
      ["CredentialId"]
    skipHeader: true
    order: sequence

scenarios:
  - name: "Non-success API requests to /credentials/{id} endpoint"
    flow:
    # Case: Invalid URL
      - post:
          url: "/invalid_url"
          expect:
            - statusCode: 404

    # Pause to simulate user think time
      - think: 5
      
    # Case: DELETE to /credentials/{id} without valid 'Token' header
      - delete:
          url: "/credentials/{{ CredentialId }}"
          headers:
            Token: "invalid_token"
          expect:
            - statusCode: 401
  
    # Pause to simulate user think time
      - think: 5

    # Case: DELETE to /credentials/{id} with valid 'Token' header but nonexistent 'id'
      - delete:
          url: "/credentials/nonexistent_id"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404

    # Pause to simulate user think time
      - think: 5

    # Case: DELETE to /credentials/{id} with valid 'Token' header and id, expecting 200 - 299 status code
      - delete:
          url: "/credentials/{{ CredentialId }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - notStatusCode: 200

    # Pause to simulate user think time
      - think: 5
