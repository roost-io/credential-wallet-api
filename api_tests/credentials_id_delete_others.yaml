# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /credentials/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=f1fc1d06f7


config: 
  target: "{{$processEnvironment.API_HOST}}"
  phases:
    - arriveRate: 1
      name: "Test Scenario - Non-Success API Responses"
      duration: 60
  plugins:
    expect: {} # Enabled expect plugin
  payload:
    path: "./data/credentials_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true # Skip the header line of the CSV file

scenarios: # A list containing all Scenarios
  - name: "Focusing on various non-successful responses"
    flow: 
      - # Request 'delete' to incorrect API path without any Authorization token; expecting unauthorized status
        get: 
          url: "/invalid-path/{{ id }}"
          expect:
            - statusCode: 401
          think: 2

      - # Request 'delete' to correct API but without any Authorization token; expecting unauthorized status
        get: 
          url: "/credentials/{{ id }}"
          expect:
            - statusCode: 401
        think: 2

      - # Request 'delete' to correct API with invalid Authorization token; expecting unauthorized status
        delete: 
          url: "/credentials/{{ id }}"
          headers:
            Token: "Invalid-Token" # Invalid Authorization token
          expect: 
            - statusCode: 401
          think: 2

      - # Request 'delete' to correct API with correct Authorization token but invalid id; expecting not found status
        delete: 
          url: "/credentials/invalid-id" 
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}" # Correct Authorization token
          expect: 
            - statusCode: 404 # Not found
          think: 2

      - # Request 'delete' to correct API with correct Authorization token and id; expecting success status
        delete: 
          url: "/credentials/{{ id }}" 
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}" # Correct Authorization token
          expect: 
            - statusCode: 200 # Success
          think: 2

