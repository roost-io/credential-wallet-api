# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=f1fc1d06f7

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "credentials_id_delete_others.csv"
    fields:
      - "id"
  plugins:
    expect: {}

scenarios:
  - name: "Test Non-Success API Responses"
    flow:
      - log: "Executing DELETE request to delete a credential"
        delete:
          url: "/v1/credentials/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
              hasHeader: "api-version"
        think: 2

      - log: "Executing DELETE request to non-existing endpoint"
        delete:
          url: "/v1/unexistent-endpoint/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 2

      - log: "Executing DELETE request with an invalid id"
        delete:
          url: "/v1/credentials/invalid-id"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
              hasHeader: "api-version"
        think: 2

      - log: "Executing DELETE request without authentication"
        delete:
          url: "/v1/credentials/{{ id }}"
          expect:
            - statusCode: 401
        think: 2

      - log: "Executing DELETE request with an invalid authentication token"
        delete:
          url: "/v1/credentials/{{ id }}"
          headers:
            Token: "invalid_auth_token"
          expect:
            - statusCode: 403
              hasHeader: "api-version"
        think: 2
