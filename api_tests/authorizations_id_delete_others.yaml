# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=cb128291e8


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "authorizations_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: Non-success API Response Test Scenario
    flow:
      - post: 
          url: "/authorizations/incorrect_id"  
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
          think: 1
      - get: 
          url: "/nonexistent_authorizations/{{ id }}"  
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
          think: 1
      - log: 'Testing invalid URL'
      - get: 
          url: "/nonexistent_endpoint"
          expect:
            - statusCode: 404
          think: 1
      - delete:
          url: "/authorizations/{{ id }}"  
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasHeader: "Content-Type"
            - contentType: "application/json"
            - hasProperty: "message"
          think: 1
