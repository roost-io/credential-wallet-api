# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=cb128291e8



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "authorizations_id_delete_others.csv"
    fields: ["Authorization id"]
    options:
      skipHeader: true
  environments:
    test:
      API_HOST: "http://api.example.com"

scenarios:
  - name: "Non-successful API requests test scenario"
    flow:
      - delete:  # Deleting an authorization entry with an incorrect token
          url: "/authorizations/{{ Authorization id }}"
          headers:
            Token: "Incorrect-token-sim"
          expect:
            - statusCode: 401  # Unauthorized
          think: 1
      - delete:  # Deleting an authorization entry that doesn't exist
          url: "/authorizations/00000000-0000-0000-0000-000000000000"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404  # Not Found
          think: 1
      - delete:  # Deleting an authorization entry with an invalid UUID
          url: "/authorizations/invalid-uuid-sim"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400  # Bad Request
          think: 1
      - delete:  # Deleting an authorization entry with an incorrect URL
          url: "/authorize/{{ Authorization id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404  # Not Found
          think: 1
