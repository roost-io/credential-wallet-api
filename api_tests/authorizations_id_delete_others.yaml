# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=cb128291e8


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "authorizations_id_delete_others.csv"
    fields: 
      - "id"
  processors: {}

scenarios:
  - name: "Remove Authorized Account - Edge Cases"
    flow:
      - log: "Test Case 1: Delete authorized account using invalid id"
        delete: "/authorizations/{{ id }}"
        headers:
          Token: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
        think: 1

      - log: "Test Case 2: Delete authorized account without providing Token in header"
        delete: "/authorizations/{{ id }}"
        expect:
          - statusCode: 401
        think: 1

      - log: "Test Case 3: Delete authorized account using invalid URL"
        delete: "/auth/{{ id }}"
        headers:
          Token: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
        think: 1
