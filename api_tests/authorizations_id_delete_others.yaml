# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=cb128291e8

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    - path: "authorizations_id_delete_others.csv"
      fields:
        - id
      skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "API Testing - Non-successful cases"
    flow:
      - log: "Testing invalid authorization token"
        request:
          method: "DELETE"
          url: "/authorizations/{{ id }}"
          headers:
            Token: "invalid_token"
        # Expect a 401 response due to invalid token
        expect:
          - statusCode: 401
          - hasHeader: "api-version"
          - matchesRegexp:
              api-version: "1.0.0"
        think: 1
      
      - log: "Testing with invalid ID"
        request:
          method: "DELETE"
          url: "/authorizations/invalid_id"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
        # Expect a 400 response due to invalid ID
        expect:
          - statusCode: 400
          - hasHeader: "api-version"
          - matchesRegexp:
              api-version: "1.0.0"
        think: 1

      - log: "Testing with non-existant API endpoint"
        request:
          method: "DELETE"
          url: "/non_existant_endpoint/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
        # Expect a 404 response due to non-existant endpoint
        expect:
          - statusCode: 404
        think: 1
