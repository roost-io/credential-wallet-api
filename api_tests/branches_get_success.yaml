# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches_get for http method type GET in artillery framework

# RoostTestHash=74a40da00e


config:
  target: {{ $processEnvironment.API_HOST }}
  environments:
    default:
      target: {{ $processEnvironment.API_HOST }}
  processor: "./my-processor.js"
  plugins:
    expect: {}
  payload:
    path: "branches_get_success.csv"
    skipHeader: true
    fields: 
      - "coc-number"
      - "coc-branch-number"
      - "trade-name"
      - "size"
      - "offset"
scenarios:
  - name: "Test Successful API requests"
    flow:
      - log: "Starting the GET request"
      - get:
          url: "/recipients/branches?coc-number={{ coc-number }}&coc-branch-number={{ coc-branch-number }}&trade-name={{ trade-name }}&size={{ size }}&offset={{ offset }}" 
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 200
          - hasHeader: "api-version"
          - contentType: "application/json"
          - hasProperty: "total"
          - hasProperty: "branches"
      - think: 1
      - log: "Starting the GET request with UUID"
      - get: 
          url: "/recipients/branches/497f6eca-6276-4993-bfeb-53cbbbba6f08"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 200
          - hasHeader: "api-version"
          - contentType: "application/json"
          - hasProperty: "id"
          - hasProperty: "email"
          - hasProperty: "phone"
          - hasProperty: "cocNumber"
          - hasProperty: "cocBranchNumber"
          - hasProperty: "tradeName"
      - think: 1
      - log: "Starting the GET request with UUID and registrations"
      - get:
          url: "/recipients/branches/497f6eca-6276-4993-bfeb-53cbbbba6f08/registrations"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 200
          - hasHeader: "api-version"
          - contentType: "application/json"
      - think: 1
