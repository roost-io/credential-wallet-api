# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches_get for http method type GET in artillery framework

# RoostTestHash=74a40da00e


# Artillery test script in YAML
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  ensure:
    p95: 200
  plugins:
    expect: {}
  payload:
    path: "branches_get_success.csv"
    fields:
      - "coc-number"
      - "coc-branch-number"
      - "trade-name"
      - "size"
      - "offset"
  headers:
    Token: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Test GET /recipients/branches"
    flow:
      # GET /recipients/branches with all query parameters
      - get:
          url: "/recipients/branches?coc-number={{ coc-number }}&coc-branch-number={{ coc-branch-number }}&trade-name={{ trade-name }}&size={{ size }}&offset={{ offset }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasHeader: "page-size"
            - hasHeader: "page-offset"
            - hasProperty: "branches"
            - hasProperty: "branches[0].id"
            - hasProperty: "branches[0].email"
            - hasProperty: "branches[0].phone"
            - hasProperty: "branches[0].cocNumber"
            - hasProperty: "branches[0].cocBranchNumber"
            - hasProperty: "branches[0].tradeName"
            - hasProperty: "branches[0].tradeNames"
            - hasProperty: "branches[0].eoriNumber"
            - hasProperty: "branches[0].deregistrationDate"
            - hasProperty: "branches[0].createdAt"
            - hasProperty: "branches[0].updatedAt"
            - hasProperty: "total"
      - think: 1
