# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches_get for http method type GET in artillery framework

# RoostTestHash=74a40da00e


config:
  target: "{{ $processEnvironment.API_HOST }}"
  payload:
    path: "branches_get_success.csv"
    fields:
      - "cocNumber"
      - "cocBranchNumber"
      - "tradeName"
      - "size"
      - "offset"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - engine: "http"
    flow:
      - log: "Performing branch get request with valid inputs"
        get:
          url: "/recipients/branches?coc-number={{ cocNumber }}&trade-name={{ tradeName }}&size={{ size }}&offset={{ offset }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "branches"
            - hasProperty: "total"
            - hasHeader: "api-version"
            - headerEquals:
                header: "api-version"
                value: "1.0.0"
        think: 2
