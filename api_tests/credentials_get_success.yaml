# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /credentials_get for http method type GET in artillery framework

# RoostTestHash=c5011f3c64


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "credentials_get_success.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
  environments:
    test:
      target: "{{ $processEnvironment.API_HOST }}"

scenarios: 
  - name: "Successful Authenticated API Requests"
    flow: 
      # Login user and retrieve access token
      - post: 
          url: "/login"
          json:
            email: "{{ username }}"
            password: "{{ password }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect: 
            - statusCode: 200
            - hasProperty: 'access_token'
          capture:
            json: "$.access_token"
            as: "access_token"
            
      - think: 3
      
      # Test successful /credentials endpoint
      - get:
          url: "/credentials?size=20&offset=0"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: ["credentials", "total"]
            - hasProperty: 'credentials[0].id'
            - hasProperty: 'credentials[0].format'
            - hasProperty: 'credentials[0].credential'
            - hasProperty: 'credentials[0].status'
            - hasProperty: 'credentials[0].statusUpdatedAt'
            - hasProperty: 'credentials[0].createdAt'
      
      - think: 3
