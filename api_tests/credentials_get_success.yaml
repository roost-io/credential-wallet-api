# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /credentials_get for http method type GET in artillery framework

# RoostTestHash=c5011f3c64


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload: 
    path: "credentials_get_success.csv"
    fields: 
      - "username" 
      - "password"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "User credentials tests"
    flow:
      # Get all credentials
      - get:
          url: "/credentials?size=20&offset=0"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasProperty: ["credentials", "total"]
            - equals: 
                - "total": 1
            - hasProperty: ["credentials.[0].createdAt", "credentials.[0].credential", "credentials.[0].format", "credentials.[0].id", "credentials.[0].status", "credentials.[0].statusUpdatedAt"]
      - think: 3
      
      # Get specific credential
      - get:
          url: "/credentials/497f6eca-6276-4993-bfeb-53cbbbba6f08"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasProperty: ["credentials.[0].createdAt", "credentials.[0].credential", "credentials.[0].format", "credentials.[0].id", "credentials.[0].status", "credentials.[0].statusUpdatedAt"]
      - think: 3

      # Non existent credential
      - get:
          url: "/credentials/497f6eca-6276-4993-bfeb-5"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - notStatusCode: 404
      - think: 3
