# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /credentials_get for http method type GET in artillery framework

# RoostTestHash=c5011f3c64


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "credentials_get_success.csv"
    fields: ["userId", "password"]
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: API Behavior Test
    flow:
      - get:
          url: "/credentials?size={{ $randomNumber(1,50) }}&offset={{ $randomNumber(0,20) }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:         
            - statusCode: 200  # success response
            - hasProperty: 'credentials'
            - hasProperty: 'total'
      - log: "Fetching all credentials was successful"
      - think: 1

      - get:
          url: "/credentials/{{ userId }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200  # success response
            - hasProperty: 'id'
            - hasProperty: 'format'
            - hasProperty: 'credential'
            - hasProperty: 'status'
            - hasProperty: 'statusUpdatedAt'
            - hasProperty: 'createdAt'
      - log: "Fetching single credential was successful"
      - think: 1
