# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=e0cf5b2e0e



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  # Enable expect plugin
  plugins:
    expect: {}
  # Load payload variables from CSV
  payload:
    path: "login_post_others.csv"
    fields:
      - "client"
      - "email"
      - "otp"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 5
    
scenarios:
  - name: "Login using OTP"
    flow:
      # Invalid scenario with incorrect client - Expect 401 error
      - post:
          url: "/login"
          headers:
            Content-Type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json: 
            client: "dummy"
            email: "{{ email }}"
            otp: "{{ otp }}"
          expect:
            - statusCode: 401
            - hasHeader: "content-type"
            - headerEquals: {"content-type": "application/json"}
        think: 1

      # Invalid scenario with incorrect OTP - Expect 402 error
      - post:
          url: "/login"
          headers:
            Content-Type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json: 
            client: "{{ client }}"
            email: "{{ email }}"
            otp: "dummy"
          expect:
            - statusCode: 402
            - hasHeader: "content-type"
            - headerEquals: {"content-type": "application/json"}
        think: 1

      # Invalid scenario with incorrect email - Expect 403 error
      - post:
          url: "/login"
          headers:
            Content-Type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json: 
            client: "{{ client }}"
            email: "dummy@dummy.com"
            otp: "{{ otp }}"
          expect:
            - statusCode: 403
            - hasHeader: "content-type"
            - headerEquals: {"content-type": "application/json"}
        think: 1
