# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /login_post for http method type POST in artillery framework

# RoostTestHash=e0cf5b2e0e


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "login_post_others.csv"
    fields:
      - "client"
      - "email"
      - "otp"
    skipHeader: true
  environments:
    production:
      target: "{{ $processEnvironment.API_HOST }}"
      security:
         - name: "Token"
           placeholder: "{{ $processEnvironment.AUTH_TOKEN }}"
  phases:
    - duration: 60
      arrivalRate: 1
  
scenarios:
  - name: "POST /login Failed Cases Testing"
    flow:
      - log: "Invalid email format / Login unsuccessful"
        post:
          url: "/login"
          headers:
            content-type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            client: "{{ client }}"
            email: "abc"
            otp: "{{ otp }}"
          expect:
            - statusCode: 422            # Assuming 422 for validation errors
            - hasProperty: "message"     # Assuming 'message' in error response
     
      - think: 1

      - log: "OTP incorrect / Login unsuccessful"
        post:
          url: "/login"
          headers:
            content-type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            client: "{{ client }}"
            email: "{{ email }}"
            otp: "wrongotp"              # Incorrect OTP
          expect:
            - statusCode: 401             # Assuming 401 for incorrect OTP
            - hasProperty: "error"       # Assuming 'error' in error response
      
      - think: 1

      - log: "Missing required fields / Login unsuccessful"
        post:
          url: "/login"
          headers:
            content-type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            client: "{{ client }}"
            email: "{{ email }}"         # Missing OTP field
          expect:
            - statusCode: 400             # Assuming 400 for missing required fields
            - hasProperty: "details"    # Assuming 'details' in error response
            
      - think: 1
