# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=e0cf5b2e0e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "login_post_others.csv"
    fields:
      - "client"
      - "email"
      - "otp"
    skipHeader: true

scenarios:
  - name: "Login Test - Failures"
    flow:
      - post:
          url: "/login"
          headers:
            content-type: "application/json"
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            client: "{{ client }}"
            email: "{{ email }}"
            otp: "{{ otp }}"
          expect:
            - statusCode: 400 # Expecting Bad Request for invalid inputs
            - hasHeader: "api-version"
            - hasProperty: "error" # Expecting Error object in response

      - think: 1

      - put:
          url: "/invalidurl"
          headers:
            content-type: "application/json"
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404 # Expecting Not Found for non-existent URL

      - think: 1

      - delete:
          url: "/login"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 405 # Expecting Method Not Allowed for inappropriate method type

      - think: 1

      - get:
          url: "/login"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400 # Expecting Bad Request for missing required parameters
