# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /login_post for http method type POST in artillery framework

# RoostTestHash=e0cf5b2e0e


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "./login_post_others.csv"
    fields:
      - "client"
      - "email"
      - "otp"
  variables:
    authToken: "{{$processEnvironment.AUTH_TOKEN}}"
scenarios:
  - name: "Unsuccessful API responses test scenario"
    flow:
      # Testing Invalid Method Request
      - post:
          url: "/login"
          headers:
            Token: "{{ authToken }}"
          json:
            client: "{{ client }}"
            email: "{{ email }}"
            otp: "{{ otp }}"
          expect:
            - statusCode: 405
          afterResponse: "think"
      # Testing Missing Required Field Payload
      - post:
          url: "/login"
          headers:
            Token: "{{ authToken }}"
          json:
              client: "{{ client }}"
              email: "{{ email }}"
          expect:
            - statusCode: 400
          afterResponse: "think"
      # Testing Invalid Login Credentials 
      - post:
          url: "/login"
          headers:
            Token: "{{ authToken }}"
          json:
            client: "{{ client }}"
            email: "{{ email }}"
            otp: "wrong_otp"
          expect:
            - statusCode: 401
          afterResponse: "think"
      # Testing Invalid Resource Path
      - post:
          url: "/invalidPath"
          headers:
            Token: "{{ authToken }}"
          json:
            client: "{{ client }}"
            email: "{{ email }}"
            otp: "{{ otp }}"
          expect:
            - statusCode: 404
          afterResponse: "think"
