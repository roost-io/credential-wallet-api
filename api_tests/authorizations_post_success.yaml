# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /authorizations_post for http method type POST in artillery framework

# RoostTestHash=ed8c1b7bff


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "authorizations_post_success.csv"
    fields:
      - "email"
    skipHeader: true
  headers:
    Token: "{{ $processEnvironment.AUTH_TOKEN }}"
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - name: "Get authorizations test flow"
    flow:
      - log: "Starting authorizations POST request"
      - post:
          url: "/authorizations"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "email"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
            - hasHeader: "api-version"
            - hasHeader: "location"

      - think: 5
