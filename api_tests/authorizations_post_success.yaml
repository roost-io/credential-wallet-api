# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /authorizations_post for http method type POST in artillery framework

# RoostTestHash=ed8c1b7bff


config:
  target: "{{ $processEnvironment.API_HOST }}"
  payloads:
    - path: "authorizations_post_success.csv"
      fields:
        - "email"
      skipHeader: true
  headers:
    Token: "{{$processEnvironment.AUTH_TOKEN}}"
  processor: "processors.js"
  plugins:
    fuzzer: {}
    expect: {}
scenarios:
  - name: "api-requests-scenario"
    flow:
      - post: 
          # Making a POST request to /authorizations endpoint to create a new account
          url: "/authorizations"
          headers:
            Content-Type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasProperty: ["createdAt", "email", "id", "name", "updatedAt"]
            - hasHeader: ["api-version", "location"]
            - contentType:
                equals: "application/json"
      - think: 1
