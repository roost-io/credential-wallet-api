# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /me_get for http method type GET in artillery framework
# RoostTestHash=ae6b8ce5ab



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "me_get_success.csv"
    fields:
      - "id"
      - "image"
      - "email"
      - "settings"
      - "createdAt"
      - "updatedAt"
    skipHeader: true
  environments:
    target:
      variables:
        API_KEY: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "API validation for /me endpoint"
    flow:

      # Validate successful GET /me request
      - get:
          url: "/me"
          headers:
            Authorization: "Bearer {{ API_KEY }}"
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasProperty: "id"
            - hasProperty: "image"
            - hasProperty: "email"
            - hasProperty: "settings"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
        think: 1

