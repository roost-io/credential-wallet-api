# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /me_get for http method type GET in artillery framework

# RoostTestHash=ae6b8ce5ab


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  environments:
    local:
      target: "{{ $processEnvironment.API_HOST }}"
  payload:
    path: "me_get_success.csv"
    fields: 
      - "email"
      - "did"
      - "name"
      - "settings"
      - "image"
      - "id"
      - "updatedAt"
      - "createdAt"
    skipHeader: true

scenarios:
  - name: "Successful /me API endpoint flows"
    flow:
      - log: "Starting scenario. Sending GET request to /me endpoint"
      - get:
          url: "/me"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          capture:
            - json: "$.id"
              as: "id"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
        think: 1

      - post:
          url: "/me"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            email: '{{ email }}'
            did: '{{ did }}'
            name: '{{ name }}'
            image: '{{ image }}'
            settings: '{{ settings }}'
          capture:
            - json: "$.id"
              as: "id"
          expect:
            - statusCode: 200
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
            - hasProperty: "settings"
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
        think: 1

      - put:
          url: "/me/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            email: '{{ email }}'
            did: '{{ did }}'
            name: '{{ name }}'
            image: '{{ image }}'
            settings: '{{ settings }}'
            createdAt: '{{ createdAt }}'
            updatedAt: '{{ updatedAt }}'
          expect:
            - statusCode: 200
            - hasProperty: "updatedAt"
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
        think: 1

      - delete:
          url: "/me/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
        think: 1

      - log: "Finished scenario. Successfully tested all API requests for /me endpoint."
