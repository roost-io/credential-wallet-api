# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /grants_get for http method type GET in artillery framework
# RoostTestHash=692c6b853f



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "grants_get_others.csv"
    skipHeader: true
    fields:
      - "field1"
      - "field2"
  variables:
    auth_key: "{{$processEnvironment.AUTH_TOKEN}}"
  http:
    pool: 10
    timeout: 30
  ensure:
    p99: 200


scenarios: 
  - name: "Test non-success API responses"
    flow:
      # Test for Invalid URL request
      - get:
          url: "/invalid_url"
          expect:
            - statusCode: 404
          think: 1

      # Test for Unauthorized request
      - post:
          url: "/grants"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
          expect:
            - statusCode: 401 
          think: 1

      # Test for Bad request
      - post:
          url: "/grants"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            field1: "invalidData"
            field2: "invalidData"
          expect:
            - statusCode: 400 
          think: 1

      # Test for Server error due to incorrect payload
      - post:
          url: "/grants"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            field3: "invalidData"
          expect:
            - statusCode: 500 
            - hasProperty: "message"
            - equals: ["message", "Internal server error"]
          think: 1
