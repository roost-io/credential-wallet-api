# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=692c6b853f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 600
      arrivalRate: 50
  plugins:
    expect: {}
  payload:
    path: "./payloads/grants_get_others.csv"
    fields:
      - "AuthToken"
      - "TokenHeader"
      - "IncorrectUrl"
      

scenarios:
  - name: "API Failure Scenarios"
    flow:
      - log: "Firing request with invalid token in header"
        post:
          url: "/grants"
          headers:
            Token: "InvalidToken"
          expect:
            - statusCode: 401
            - contentType: "application/json"
          think: 3

      - log: "Firing request with no token in header"
        post:
          url: "/grants"
          headers:
            Token: ""
          expect:
            - statusCode: 401
            - contentType: "application/json"
          think: 3

      - log: "Firing the request on an incorrect URL"
        post:
          url: "/invalidurl"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
          think: 3

      - log: "Sending a POST request, expecting unauthorized error (401)"
        post:
          url: "/grants"
          headers:
            Token: "{{ TokenHeader }}"
          payload:
            AuthToken: "{{ AuthToken }}"
          expect:
            - statusCode: 401
          think: 3
          
      - log: "Sending a GET request to incorrect URL, expecting not found error (404)"
        get:
          url: "{{ IncorrectUrl }}"
          expect:
            - statusCode: 404
          think: 3     
