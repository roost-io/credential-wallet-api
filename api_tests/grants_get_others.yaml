# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /grants_get for http method type GET in artillery framework

# RoostTestHash=692c6b853f


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
   - duration: 300
     arrivalRate: 5
  payload:
    path: "grants_get_others.csv"
    fields:
      - "field1"
      - "field2"
    skipHeader: true

scenarios:
  - name: "Testing non-successful API requests"
    flow:
      # Invalid GET request due to incorrect API usage
      - get:
          url: "/brokenurl"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      # Even if Authorization token is wrong, the server should generate 401 Status
      - get:
          url: "/grants"
          headers:
            Token: "Incorrect_Token"
          expect:
            - statusCode: 401
      - think: 1

      # GET request fails due to missing headers
      - get:
          url: "/grants"
          headers:
            Token: ""
          expect:
            - statusCode: 400
            - hasHeader: "content-type"
            - headerEquals: { "content-type": "application/json" }
            - hasProperty: "message"
            - hasProperty: "error"
      - think: 1

      # Sending an Unathorized Request
      - get:
          url: "/grants"
          expect:
            - statusCode: 401
            - hasHeader: "content-type"
            - headerEquals: { "content-type": "application/json" }
            - hasProperty: "message"
            - hasProperty: "error"
      - think: 1
