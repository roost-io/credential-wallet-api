# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /me_patch for http method type PATCH in artillery framework
# RoostTestHash=6dd436357d



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "me_patch_others.csv"
    fields:
      - "name"
      - "image"
      - "settings"
    skipHeader: true
  plugins: 
    expect: {}

scenarios:
  - name: "Test failure response"
    flow:
      - comment: "Send unauthenticated request to /me endpoint"
        patch:
          url: "/me"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
        expect:
          - statusCode: 401
        think: 1

      - comment: "Send authenticated request to /me endpoint with invalid payload"
        patch:
          url: "/me"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "invalid"
        expect:
          - statusCode: 400
        think: 1

      - comment: "Send authenticated request to /me endpoint with invalid token"
        patch:
          url: "/me"
          headers:
            Token: "invalid"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
        expect:
          - statusCode: 403
        think: 1

      - comment: "Send authenticated request to /me endpoint with correct payload and valid token"
        patch:
          url: "/me"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
        expect:
          - statusCode: 200
          - hasProperty: "name"
          - hasProperty: "image"
          - hasProperty: "settings"
        think: 1

      - comment: "Send authenticated request to an invalid /wrong endpoint"
        patch:
          url: "/wrong"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
        expect:
          - statusCode: 404
        think: 1
