# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /me_patch for http method type PATCH in artillery framework

# RoostTestHash=6dd436357d


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "me_patch_others.csv"
    skipHeader: true
    fields:
      - "name"
      - "image"
      - "settings"

scenarios:
  - name: "Negative tests on /me API endpoint"
    flow:
      # Testing GET without authorization
      - get:
          url: "/me"
          expect:
            - statusCode: 401 # Unauthorized
      - think: 1

      # Testing PATCH without authorization
      - post:
          url: "/me"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 401 # Unauthorized
      - think: 1

      # Testing PATCH with invalid image format
      - post:
          url: "/me"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "invalid_image"
            settings: "{{ settings }}"
          expect:
            - statusCode: 400 # Bad Request
      - think: 1

      # Testing PATCH with invalid settings
      - post:
          url: "/me"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "invalid_settings"
          expect:
            - statusCode: 400  # Bad Request
      - think: 1
