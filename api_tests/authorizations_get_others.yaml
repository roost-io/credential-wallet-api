# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /authorizations_get for http method type GET in artillery framework

# RoostTestHash=3b64f22044


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "path/to/authorizations_get_others.csv"
    fields:
      - "field1"
      - "field2"

scenarios:
  - name: "Test scenario for non-success API responses"
    flow:
      - log: "Testing /authorizations endpoint with invalid API key"
        get:
          url: "/authorizations"
          headers:
            Token: "invalid_api_key"
          expect:
            - statusCode: 401
        think: 1

      - log: "Testing /authorizations endpoint with missing API key"
        get:
          url: "/authorizations"
          headers:
            Token: ""
          expect:
            - statusCode: 401
        think: 1

      - log: "Testing /authorizations/:id endpoint with nonexistent id"
        get:
          url: "/authorizations/497f6eca-6276-4993-bfeb-53cbbbba6f08"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1

      - log: "Testing /authorizations/:id endpoint with invalid format of id"
        get:
          url: "/authorizations/invalid_id_format"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
        think: 1

      - log: "Testing /authorizations/:id endpoint with missing id"
        get:
          url: "/authorizations/"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1

      - log: "Testing with invalid URL"
        get:
          url: "/invalid_url"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1
