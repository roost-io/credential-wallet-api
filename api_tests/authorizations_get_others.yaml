# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /authorizations_get for http method type GET in artillery framework

# RoostTestHash=3b64f22044


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "authorizations_get_others.csv"
    fields:
      - "id"
      - "name"
  plugins:
    expect: {}

scenarios:
  - name: Non-Success API Responses
    flow:
      # Testing incorrect URL
      - get:
          url: "/fakeEndpoint"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
          afterResponse: "think"
      # Testing correct URL without authorization 
      - get:
          url: "/authorizations"
          expect:
            - statusCode: 401
            - hasProperty: "detail"
            - equals: 
                - "detail: Invalid token."
          afterResponse: "think"
      # Testing correct URL with incorrect authorization method
      - get:
          url: "/authorizations"
          headers:
            WrongName: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 401
            - hasProperty: "detail"
            - equals: 
                - "detail: Invalid token."
          afterResponse: "think"
      # Testing a specific item from the payload CSV file      
      - get:
          url: "/authorizations/{{ id }}"
          headers:
            Token: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 401
            - hasProperty: "detail"
            - equals: 
                - "detail: Record not found."
