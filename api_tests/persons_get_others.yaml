# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/persons_get for http method type GET in artillery framework

# RoostTestHash=aa0a5d4980


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  # Payload for testing
  payload:
    path: "persons_get_others.csv"
    fields:
      - "size"
      - "offset"
      - "name"
      - "birth-date"
    skipHeader: true

scenarios:
  - flow:
      # Incorrect URL
      - get:
          url: "/not-valid-url"
          afterResponse: "think: 1"
          # Check for 404 error code
          expect:
            - statusCode: 404
      # Incorrect token
      - get:
          url: "/recipients/person"
          headers: 
            Token: "invalid_token"
          afterResponse: "think: 1"
          # Check for 401 error code
          expect:
            - statusCode: 401
      # Get request with incorrect payload
      - get:
          url: "/recipients/person?size={{ size }}&offset={{ offset }}&name={{ name }}&birth-date={{ birth-date }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          afterResponse: "think: 1"
          # Check for 400 error code
          expect:
            - statusCode: 400
      # Test for unsuccessful response when all parameters are incorrect
      - get:
          url: "/recipients/person?size=999&offset=-1&name=invalid&birth-date=0000-00-00"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          afterResponse: "think: 1"
          # Check for 200 status code
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: "results"
            - "!hasProperty": "results[0]"
