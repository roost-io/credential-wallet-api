# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/persons_get for http method type GET in artillery framework

# RoostTestHash=aa0a5d4980


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "persons_get_others.csv"
    fields: ["name", "birth_date"]
    skipHeader: true

scenarios:
  - name: "Various non-successful requests"
    flow:

      # Sending an invalid GET request to the API endpoint (missing required parameters)
      - get:
          url: "/recipients/persons"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400

      - think: 1

      # Sending a GET request with invalid authorization
      - get:
          url: "/recipients/persons?name={{ name }}&birth-date={{ birth_date }}"
          headers:
            Token: "InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: 'error'
            - hasHeader: 'content-type'
            - equals:
                - 'content-type': 'application/json'

      - think: 1

      # Sending a GET request with incorrect URL
      - get:
          url: "/incorrectEndpoint?name={{ name }}&birth-date={{ birth_date }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404

      - think: 1

      # Sending a GET request with valid parameters and correct authorization but expecting incorrect status code 
      - get:
          url: "/recipients/persons?name={{ name }}&birth-date={{ birth_date }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - notStatusCode: 200
