# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /recipients/persons_post for http method type POST in artillery framework
# RoostTestHash=b4614c0581



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  defaults:
    headers:
      Token: "{{$processEnvironment.AUTH_TOKEN}}"
  environments:
    healthCheck:
      target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "persons_post_success.csv"
    fields: ["birthDate", "initials", "firstName", "middleName", "lastName", "birthPlace", "phone", "email", "postalCode", "houseNumber", "houseNumberPostfix", "countryCode", "reference"]
    skipHeader: true

scenarios:
  - name: "Validate successful person API responses"
    flow:
      # Creating new Person
      - post:
          url: "/recipients/persons"
          headers:
            Content-Type: "application/json"
          json:
            birthDate: "{{ birthDate }}"
            initials: "{{ initials }}"
            firstName: "{{ firstName }}"
            middleName: "{{ middleName }}"
            lastName: "{{ lastName }}"
            birthPlace: "{{ birthPlace }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
            postalCode: "{{ postalCode }}"
            houseNumber: "{{ houseNumber }}"
            houseNumberPostfix: "{{ houseNumberPostfix }}"
            countryCode: "{{ countryCode }}"
            reference: "{{ reference }}"
          expect:
            - statusCode: 201
            - hasProperty: ["initials", "firstName", "middleName", "lastName", "birthPlace", "phone", "email", "postalCode", "houseNumber", "houseNumberPostfix", "countryCode", "reference"]
            - hasHeader: ["api-version", "location"]
        think: 1
