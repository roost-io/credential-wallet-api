# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/persons_post for http method type POST in artillery framework

# RoostTestHash=b4614c0581


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "persons_post_success.csv"
    skipHeader: true

scenarios:
  - name: "Recipient Person Test Scenario"
    flow:
      - post:
          url: "/recipients/persons"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
            content-type: "application/json"
          json:
            birthDate: "{{ birthDate }}"
            initials: "{{ initials }}"
            lastName: "{{ lastName }}"
            # rest of the fields are populated from persons_post_success.csv
          expect:
            - statusCode: 201
            - hasProperty: "birthDate"
            - hasProperty: "initials"
            - hasProperty: "lastName"
            - headers:
                api-version:
                  equals: "1.0.0"
                location:
                  matchesRegexp: "/.*?"
          think: 1
