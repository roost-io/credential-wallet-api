# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=1ce4bf6e3c


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  headers:
    Token: "{{ $processEnvironment.AUTH_TOKEN }}"
  payload:
    path: "./branches_id_delete_success.csv"
    fields:
      - id
    skipHeader: true

scenarios:
  - name: Validate successful API requests
    flow:
      # Get API with given id
      - get:
          url: "/recipients/branches/{{ id }}"
          expect:
            - statusCode: 200
            - hasHeader: api-version
            - body:
                hasProperty: id
        think: 5
      
      # Post API with given id
      - post:
          url: "/recipients/branches/{{ id }}"
          expect:
            - statusCode: 201
            - body:
                hasProperty: id
        think: 5
      
      # Update API with given id
      - put:
          url: "/recipients/branches/{{ id }}"
          expect:
            - statusCode: 200
            - body:
                hasProperty: id
        think: 5
      
      # Delete API with given id
      - delete:
          url: "/recipients/branches/{{ id }}"
          expect:
            - statusCode: 204
        think: 5
