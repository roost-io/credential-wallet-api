# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=c02d3561ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "credentials_id_get_others.csv"
    fields:
      - "id"
  plugins:
    expect: {}
    
scenarios:
  - name: "Artillery non-success tests"
    flow:
      - get:
          url: "/credentials/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          capture:
            - json: "$"
              as: "responseBody"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
        think: 5
      - get:
          url: "/invalid-url/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          capture:
            - json: "$"
              as: "responseBody"
          expect:
            - statusCode: 404
        think: 5
      - get:
          url: "/credentials/{{ id }}"
          expect:
            - statusCode: 401
        think: 5
      - post:
          url: "/credentials"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: "{{ id }}"
          expect:
            - statusCode: 405
        think: 5
