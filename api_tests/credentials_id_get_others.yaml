# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /credentials/{id}_get for http method type GET in artillery framework

# RoostTestHash=c02d3561ab


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "credentials_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: 'Non-Success API Requests Test Scenario'
    flow:
      # Incorrect API usage: Non-existing UUID
      - get:  
          url: "/credentials/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404  # Expected status code on not found records
          think: 1

      # Incorrect API usage: Invalid UUID format
      - get:  
          url: "/credentials/invalid_uuid"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400  # Expected status code on invalid request format
          think: 1

      # Incorrect API usage: Unauthorized access
      - get:  
          url: "/credentials/{{ id }}"
          headers:
            Token: "Invalid_Token"
          expect:
            - statusCode: 401  # Expected status code on unauthorized access
          think: 1

      # Incorrect API usage: Invalid URL
      - get:  
          url: "/invalid_url/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404  # Expected status code on invalid url
          think: 1
