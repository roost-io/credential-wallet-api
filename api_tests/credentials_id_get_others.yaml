# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /credentials/{id}_get for http method type GET in artillery framework

# RoostTestHash=c02d3561ab


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 120
      arrivalRate: 10
  payload:
    path: "credentials_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}
      
scenarios:
  - name: "Invalid and error scenario for API /credentials/{id}"
    flow:
      # Request with Invalid UUID
      - get:
          url: "/credentials/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400 # Bad Request
            - hasProperty: "message"
            - hasProperty: "details"
          think: 5

      # Request without auth token
      - get:
          url: "/credentials/{{ id }}"
          expect:
            - statusCode: 403 # Forbidden
          think: 5

      # Request with invalid auth token
      - get:
          url: "/credentials/{{ id }}"
          headers:
            Token: "invalid_token"
          expect:
            - statusCode: 403 # Forbidden
          think: 5

      # Request to wrong endpoint
      - get:
          url: "/cred/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404 # Not Found
          think: 5

      # Request with valid UUID but resource not found
      - get:
          url: "/credentials/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404 # Not Found
            - hasProperty: "message"
            - hasProperty: "details"
          think: 5
