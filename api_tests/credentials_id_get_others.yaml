# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /credentials/{id}_get for http method type GET in artillery framework
# RoostTestHash=c02d3561ab



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "credentials_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "Test invalid API usage"
    flow:
      # Test 1: Trying to get credential with Invalid UUID
      - get:
          url: "/credentials/{{ id }}"
          headers: 
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
        think: 1

      # Test 2: Trying to get credential with Non-existent UUID
      - get:
          url: "/credentials/123e4567-e89b-12d3-a456-426614174000"
          headers: 
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
        think: 1
