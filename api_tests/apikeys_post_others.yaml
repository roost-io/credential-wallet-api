# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=28a54f1579

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "apikeys_post_others.csv"
    fields:
      - "name"
      - "email"
      - "dob"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Test Non-Successful API Responses"
    flow:
      - post:
          url: "/apikeys"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            name: "{{ name }}"
            email: "{{ email }}"
            dob: "{{ dob }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - contentType: 'application/json'
    # Pausing for 5 seconds before repeating the flow
        think: 5
      - post:
          url: "/not_existent_url"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            name: "{{ name }}"
            email: "{{ email }}"
            dob: "{{ dob }}"
          expect:
            - statusCode: 404
            - contentType: 'application/json'
        think: 5
      - post:
          url: "/apikeys"
          headers:
            Token: "WrongTokenValue"
          json:
            name: "{{ name }}"
            email: "{{ email }}"
            dob: "{{ dob }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - contentType: 'application/json'
        think: 5
