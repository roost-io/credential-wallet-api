# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /apikeys_post for http method type POST in artillery framework

# RoostTestHash=28a54f1579


config:
  target: "{{ $processEnvironment.API_HOST }}"
  payloads:
    - name: 'apikeys'
      path: 'apikeys_post_others.csv'
      skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: 'Non-Success API Requests'
    flow:
      - put:
          url: 'notexist/apikeys'
          comments: 'Invalid URL. End Point does not exist'
          headers:
            Token: '{{$processEnvironment.AUTH_TOKEN}}'
          expect:
            - statusCode: 404
          think: 2

      - post:
          url: '/apikeys'
          comments: 'Invalid payload. payload is missing from the request'
          headers:
            Token: '{{$processEnvironment.AUTH_TOKEN}}'
          expect:
            - statusCode: 400
          think: 2

      - post:
          url: '/apikeys'
          comments: 'Invalid Authorization header. Missing Token'
          expect:
            - statusCode: 403
          think: 2

      - post:
          url: '/apikeys'
          comments: 'Negative test case, invalid field in payload.'
          json:
            invalid_field: "Invalid"
          headers:
            Token: '{{$processEnvironment.AUTH_TOKEN}}'
          expect:
            - statusCode: 400
          think: 2
