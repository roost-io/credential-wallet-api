# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /apikeys_post for http method type POST in artillery framework
# RoostTestHash=28a54f1579



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "apikeys_post_others.csv"
    fields:
      - "Token"
    skipHeader: true

scenarios:
  - name: testing API for non-success responses
    flow:
      - post: 
          # Testing case when authorization token is missing
          url: "/apikeys"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 401
          think: 2
      
      - get: 
          # Testing case for an invalid URL
          url: "/notAnEndpoint"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
          think: 2

      - post: 
          # Testing case where the payload is empty instead of the required one
          url: "/apikeys"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json: {}
          expect:
            - statusCode: 422
          think: 2

responses:
  - match:
      - failure: 
          - statusCode: 401
          - body: 'Unauthorized'
          - hasProperty: 'message'
        think: 2

      - error: 
          - statusCode: 404
          - body: 'Not Found'
          - hasProperty: 'message'
        think: 2

      - invalid: 
          - statusCode: 422
          - body: 'Invalid Payload'
          - hasProperty: 'message'
        think: 2
