# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches/{id}/registrations_get for http method type GET in artillery framework

# RoostTestHash=49b35fa9c1


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {} 
  payload:
    path: "registrations_get_others.csv"
    fields: ["id", "credentialUrl", "size", "offset"]
    skipHeader: true

scenarios:
 - name: Testing non success API responses
   flow:

     # Invalid ID
     - get:
         url: "/recipients/branches/INVALID_ID/registrations"
         headers:
           Token: "{{$processEnvironment.AUTH_TOKEN}}"
         expect:
           - statusCode: 404 # Unexpected ID results in 404 status code

     - think: 1

     # Lack of mandatory ID
     - get:
         url: "/recipients/branches//registrations"
         headers:
           Token: "{{$processEnvironment.AUTH_TOKEN}}"
         expect:
           - statusCode: 400 # missing mandatory path parameter results in 400 status code

     - think: 1

     # Incorrect host URL
     - get:
         url: "{{ $processEnvironment.API_WRONG_URL }}"
         headers:
           Token: "{{$processEnvironment.AUTH_TOKEN}}"
         expect:
           - statusCode: 400 # Incorrect URL results in 400 status code

     - think: 1

     # Unauthorized access to resource
     - get:
         url: "/recipients/branches/INVALID_ID/registrations"
         headers:
           Token: "INCORRECT_TOKEN"
         expect:
           - statusCode: 403 # Incorrect Token results in 403 status code

     - think: 1

     # Accessing an endpoint that does not exist
     - get:
         url: "/recipients/branches/{{ id }}/registrations_wrong_endpoint"
         headers:
           Token: "{{$processEnvironment.AUTH_TOKEN}}"
         expect:
           - statusCode: 404 # Incorrect endpoint results in 404 status code

     - think: 1

     # Entity too large
     - post:
         url: "/recipients/branches/{{ id }}/registrations"
         headers:
           Token: "{{$processEnvironment.AUTH_TOKEN}}"
           Content-Type: "application/json"
         json:
           data: "{{ tooLargeData }}"
         expect:
           - statusCode: 413 # Too large entity results in 413 status code
