# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /recipients/persons/{id}_patch for http method type PATCH in artillery framework
# RoostTestHash=1416aa2295



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "persons_id_patch_success.csv"
    fields:
      - "id"
      - "initials"
      - "firstName"
      - "middleName"
      - "lastName"
      - "birthDate"
      - "birthPlace"
      - "phone"
      - "email"
      - "postalCode"
      - "houseNumber"
      - "houseNumberPostfix"
      - "countryCode"
      - "reference"
  plugins:
    expect: {}
  environments:
    development:
      target: "http://localhost:4000"

scenarios:
  - name: "Update a person details test"
    flow:
      - log: "Update a person details request"
      - think: 3
      - headers:
          content-type: "application/json"
          Token: "{{$processEnvironment.AUTH_TOKEN}}"
        patch:
          url: "/recipients/persons/{{ id }}"
          json:
            initials: "{{ initials }}"
            firstName: "{{ firstName }}"
            middleName: "{{ middleName }}"
            lastName: "{{ lastName }}"
            birthDate: "{{ birthDate }}"
            birthPlace: "{{ birthPlace }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
            postalCode: "{{ postalCode }}"
            houseNumber: "{{ houseNumber }}"
            houseNumberPostfix: "{{ houseNumberPostfix }}"
            countryCode: "{{ countryCode }}"
            reference: "{{ reference }}"
        expect:
          - statusCode: 200
          - hasProperty: ["birthDate", "birthPlace", "countryCode", "createdAt", "email", "firstName", "houseNumber", "houseNumberPostfix", "id", "initials", "lastName", "middleName", "phone", "postalCode", "reference", "updatedAt"]
          - hasHeader: "api-version"

