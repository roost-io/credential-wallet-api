# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches/{id}/registrations_post for http method type POST in artillery framework

# RoostTestHash=16a6858a84


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "registrations_post_others.csv"
    fields:
      - "id"
    skipHeader: true
    order: sequence

scenarios:
  - name: "Testing various non-successful API requests"
    flow:
      - post: 
          url: "/recipients/branches/{{ id }}/registrations"
          headers:
            content-type: "application/json"
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
          comment: Testing registration with an invalid date format in startDate
          expect:
            - statusCode: 422
            - hasProperty: "errors.startDate"
          think: 1

      - post: 
          url: "/recipients/branches/{{ id }}/registrations"
          headers:
            content-type: "application/json"
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
          comment: Testing registration without sending credentialUrl
          expect:
            - statusCode: 422
            - hasProperty: "errors.credentialUrl"
          think: 1

      - post: 
          url: "/nonexistentpath/{{ id }}/registrations"
          headers:
            content-type: "application/json"
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
          comment: Testing registration to a non existent path
          expect:
            - statusCode: 404
          think: 1

      - post: 
          url: "/recipients/branches/{{ id }}/registrations"
          headers:
            content-type: "application/json"
          json:
          comment: Testing registration without sending API key in headers
          expect:
            - statusCode: 401
          think: 1

      - post: 
          url: "/recipients/branches/{{ id }}/registrations"
          headers:
            content-type: "application/json"
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
          comment: Trying to register with exceeding the max length for reference
          expect:
            - statusCode: 422
            - hasProperty: "errors.reference"
          think: 1
