# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/persons/{id}/registrations_post for http method type POST in artillery framework

# RoostTestHash=0973c499e9


config:
  target: "{{ $processEnvironment.API_HOST }}"
  payload: 
    path: "registrations_post_others.csv"
    fields:
      - "id"
      - "reference"
      - "startDate"
      - "endDate"
      - "credentialUrl"
    skipHeader: true 
  environments:
    prod:
      target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}

  scenarios:
    - name: "Unsuccessful API Requests Scenario"
      flow:
        - think: 5
        # Testing invalid Path Parameter
        - post:
            url: "/recipients/persons/invalid-uuid/registrations"
            headers:
              Content-Type: "application/json"
              Token: "{{$processEnvironment.AUTH_TOKEN}}"
            json:
              reference: "{{ reference }}"
              startDate: "{{ startDate }}"
              endDate: "{{ endDate }}"
              credentialUrl: "{{ credentialUrl }}"
            expect:
              - statusCode: 400
              - hasProperty: "message"
        - think: 5

        # Testing Invalid Body Payload
        - post:
            url: "/recipients/persons/{{ id }}/registrations"
            headers:
              Content-Type: "application/json"
              Token: "{{$processEnvironment.AUTH_TOKEN}}"
            json:
              reference: "{{ reference }}"
              startDate: "Invalid date"
              endDate: "{{ endDate }}"
              credentialUrl: "{{ credentialUrl }}"
            expect:
              - statusCode: 400
              - hasProperty: "message"
        - think: 5

        # Missing Required Fields in Payload
        - post:
            url: "/recipients/persons/{{ id }}/registrations"
            headers:
              Content-Type: "application/json"
              Token: "{{$processEnvironment.AUTH_TOKEN}}"
            json:
              reference: "{{ reference }}"
              endDate: "{{ endDate }}"
              credentialUrl: "{{ credentialUrl }}"
            expect:
              - statusCode: 400
              - hasProperty: "message"
        - think: 5

        # Inauthentic Token
        - post:
            url: "/recipients/persons/{{ id }}/registrations"
            headers:
              Content-Type: "application/json"
              Token: "Invalid Token"
            json:
              reference: "{{ reference }}"
              startDate: "{{ startDate }}"
              endDate: "{{ endDate }}"
              credentialUrl: "{{ credentialUrl }}"
            expect:
              - statusCode: 403
              - hasProperty: "message"
        - think: 5
            
        # Case that the API does not exist
        - post:
            url: "/not/exist/{{ id }}/registrations"
            headers:
              Content-Type: "application/json"
              Token: "{{$processEnvironment.AUTH_TOKEN}}"
            json:
              reference: "{{ reference }}"
              startDate: "{{ startDate }}"
              endDate: "{{ endDate }}"
              credentialUrl: "{{ credentialUrl }}"
            expect:
              - statusCode: 404
              - hasProperty: "message"
        - think: 5
