# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=16a6858a84

config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    p95: 200
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "registrations_post_others.csv"
    fields:
      - "id"
      - "reference"
      - "startDate"
      - "endDate"
      - "credentialUrl"
    skipHeader: true

scenarios:
  - name: "Test for Non-Success API Responses"
    flow:
      - log: "Making POST request with invalid payload to /recipients/branches/{id}/registrations"
      
      - post:
          url: "/recipients/branches/{{ id }}/registrations"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            reference: "{{ reference }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          expect:
            - statusCode: 400
            - hasProperty: "errors[0].title"
          think: 5

      - log: "Making POST request without required header."

      - post:
          url: "/recipients/branches/{{ id }}/registrations"
          headers: null
          json:
            reference: "{{ reference }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          expect:
            - statusCode: 401
            - hasProperty: "errors[0].title"
          think: 5

      - log: "Making POST request with invalid ID in URL."

      - post:
          url: "/recipients/branches/invalid-id/registrations"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            reference: "{{ reference }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          expect:
            - statusCode: 404
            - hasProperty: "errors[0].title"
          think: 5
