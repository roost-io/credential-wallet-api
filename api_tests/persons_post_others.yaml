# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /recipients/persons_post for http method type POST in artillery framework
# RoostTestHash=b4614c0581



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "persons_post_others.csv"
    fields:
      - "birthDate"
      - "initials"
      - "lastName"
      - "firstName"
      - "middleName"
      - "email"
      - "postalCode"
      - "houseNumber"
      - "houseNumberPostfix"
      - "countryCode"
      - "reference"
    skipHeader: true
  # Define 4 phases of 10 seconds each for our test
  phases:
    - duration: 10
      arrivalRate: 5
    - duration: 10
      arrivalRate: 5
    - duration: 10
      arrivalRate: 5
    - duration: 10
      arrivalRate: 5

scenarios:
  - name: "Testing API failure scenarios"
    flow:
      # Hitting an incorrect URL, thus expecting a 404 error
      - post:
          url: "/incorrectpath"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      # Sending an invalid payload for the post request, expecting a 400 error
      - post:
          url: "/recipients/persons"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            firstName: "John*"
            lastName: "Doe"
            initials: "JD"
            birthDate: "2000-01-01"
            postalCode: "1234AB"
            houseNumber: "12"
            houseNumberPostfix: "a"
            countryCode: "NL"
            reference: "abc"
          expect:
            - statusCode: 400
      - think: 1

      # Sending a request with missing authorization token, expecting 401 error
      - post:
          url: "/recipients/persons"
          headers:
            Token: ""
          json:
            firstName: "John"
            lastName: "Doe"
            initials: "JD"
            birthDate: "2000-01-01"
            postalCode: "1234AB"
            houseNumber: "12"
            houseNumberPostfix: "a"
            countryCode: "NL"
            reference: "abc"
          expect:
            - statusCode: 401
      - think: 1

      # Sending a request with an invalid authorization token, expecting 403 error
      - post:
          url: "/recipients/persons"
          headers:
            Token: "Invalid-Token"
          json:
            firstName: "John"
            lastName: "Doe"
            initials: "JD"
            birthDate: "2000-01-01"
            postalCode: "1234AB"
            houseNumber: "12"
            houseNumberPostfix: "a"
            countryCode: "NL"
            reference: "abc"
          expect:
            - statusCode: 403
      - think: 1
