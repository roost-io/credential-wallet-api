# ********RoostGPT********
# Test generated by RoostGPT for test credential-integration-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "scenario-803fe763f5.csv"
    fields:
      - "email"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 5
  defaults:
    headers:
      Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
      api-version: 1.0.0
      commit-hash: z1n09ub
      content-type: application/json
      accept: application/json 

scenarios:
  - name: "Creating account"
    flow:
      - post:
          url: "/accounts"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasProperty: 'api-version'
            - equals:
                headers:
                  commit-hash: "928d28d"
                  api-version: "1.0.0"
      - log: 'Account created with email: {{ email }}'
      
  - name: "Grant authorization"
    flow:
      - post:
          url: "/authorizations"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasProperty: 'id'
            - headerEquals:
                location: "string"
      - log: 'Authorization granted to account under: {{ email }}'

  - name: "Confirm Access"
    flow:
      - get:
          url: "/authorizations"
          expect:
            - statusCode: 200
            - hasProperty: 'total'
      - log: 'Access confirmed for account under: {{ email }}'
