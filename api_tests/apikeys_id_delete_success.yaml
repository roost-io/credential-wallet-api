# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /apikeys/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=4affc26394


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "apikeys_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "API Request Scenario"
    flow:
      - get:
          url: "/apikeys/{{ id }}"
          headers:
            "{{ $processEnvironment.AUTH_TOKEN }}": "{{ SECURITY_SCHEMA.ApiKeyAuth.name }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasProperty: "id"
            - equals: 
                "api-version": "1.0.0"
      - think: 3
