# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /apikeys/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=4affc26394


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "apikeys_id_delete_success.csv"
    fields: ["id"]
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: API key deletion test
    flow:
      - log: "Delete request for API key using valid UUID from CSV data"
        delete:
          url: "/apikeys/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - hasHeader: api-version
            - statusCode: 200

        think: 1

      - log: "Post request to regenerate API key after deletion"
        post:
          url: "/apikeys"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
          expect:
            - hasHeader: api-version
            - statusCode: 200

        think: 2
