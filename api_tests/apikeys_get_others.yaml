# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /apikeys_get for http method type GET in artillery framework
# RoostTestHash=9e57a3b0dd



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "apikeys_get_others.csv"
    skipHeader: true
    fields:
      - "key"
      - "username"
      - "password"

scenarios:
  - name: "Test Scenario for non-successful API requests"
    flow:
      # Invalid Request without Token in Header
      - post:
          url: "/apikeys"
          headers:
            Content-Type: "application/json"
          json:
            key: "{{ key }}"
            username: "{{ username }}"
            password: "{{ password }}"
          expect:
            - statusCode: 401 # Expecting Unauthorized status code
        think: 5
      
      # Valid Request but invalid API endpoint
      - get:
          url: "/invalidapikeys" # Incorrect endpoint
          headers:
            Content-Type: "application/json"
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404 # Expecting Not Found status code
        think: 5

      # Valid Request but expecting resource not found
      - get:
          url: "/apikeys/{{ key }}"
          headers:
            Content-Type: "application/json"
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404 # Expecting Not Found status code
        think: 5

      # Testing the response header
      - get:
          url: "/apikeys"
          headers:
            Content-Type: "application/json"
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - hasHeader: "date" # Expecting Date header in the response
            - statusCode: 200
        think: 5

      # Testing different content type
      - put:
          url: "/apikeys/{{ key }}"
          headers:
            Content-Type: "application/xml" # Sending XML instead of JSON
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          body: "<apikeys><key>{{ key }}</key><username>{{ username }}</username><password>{{ password }}</password></apikeys>"
          expect:
            - statusCode: 415 # Expecting Unsupported Media Type status code
        think: 5
