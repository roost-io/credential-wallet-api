# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /apikeys_get for http method type GET in artillery framework

# RoostTestHash=9e57a3b0dd


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "apikeys_get_others.csv"
    fields:
      - "apikey"
    skipHeader: true

scenarios:
  - name: "Non success API requests testing"
    flow:
    
      # Request with no auth header
      - log: "Get /apikeys without header"
      - get:
          url: "/apikeys"
        expect:
          - statusCode: 401
      - think: 1
      
      # Request with incorrect URL
      - log: "Incorrect URL Request"
      - get:
          url: "/apikey"
        expect:
          - statusCode: 404
      - think: 1
      
      # Request with invalid API Key
      - log: "Get /apikeys with invalid API key"
      - get:
          url: "/apikeys"
          headers:
            Token: "InvalidApiKey"
        expect:
          - statusCode: 403
      - think: 1
      
      # Request with valid API Key
      - log: "Get /apikeys with valid API key"
      - get:
          url: "/apikeys"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 200
          - hasProperty: "data"
      - think: 1
