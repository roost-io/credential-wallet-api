# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /credentials/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=f1fc1d06f7



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 10
      arrivalRate: 10
  payload:
    path: "credentials_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true
  variables:
    AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: Delete Credentials API Test Scenario
    flow:
      - comment: Delete a credential - expect a 200 status code
        delete:
          url: "/credentials/{{ id }}"
          headers:
            Token: "{{ AUTH_TOKEN }}"
        expect:
          - statusCode: 200
          - hasHeader: "api-version"
        think: 5
