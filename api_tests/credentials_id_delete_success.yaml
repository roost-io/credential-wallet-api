# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /credentials/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=f1fc1d06f7


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 600
      arrivalRate: 50
  payload:
    path: "credentials_id_delete_success.csv"
    fields: ["id"]
    skipHeader: true
  plugins:
    expect: {}
  defaults:
    headers: 
      Token: "{{$processEnvironment.AUTH_TOKEN}}"
scenarios: 
  - name: validate successful credential deletion
    flow:
      - post:
          url: "/credentials/{{ id }}"
          expect: 
            - statusCode: 200
            - hasProperty: headers['api-version']
            - equals: 
                headers['api-version']['example']: "1.0.0"
          log: "Deleting Credential ID {{ id }}, expecting successful operation with 200 status"
          think: 5
