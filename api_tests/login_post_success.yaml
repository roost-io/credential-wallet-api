# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /login_post for http method type POST in artillery framework

# RoostTestHash=e0cf5b2e0e


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
  - duration: 300
    arrivalRate: 1
  processor: "./login_script_processor.js"
  payload:
    path: "login_post_success.csv"
    skipHeader: true
    fields:
    - "client"
    - "email"
    - "otp"
  plugins:
    expect: {}

scenarios:
  - name: "Login using OTP"
    flow:
      - get:
          url: "/login"
      # Test POST API for Login using OTP, and expect a successful operation with HTTP 201 status code
      - post:
          url: "/login"
          json:
            client: "{{ client }}"
            email: "{{ email }}"
            otp: "{{ otp }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 201
            - hasProperty: ["token"]
            - hasHeader: ["api-version"]
            - equals:
              - headers.api-version: "1.0.0"
              - headers.content-type: "application/json"
      # Insert "think time" to simulate the pause between consequent requests
      - think: 5
