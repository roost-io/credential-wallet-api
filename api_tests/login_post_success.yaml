# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=e0cf5b2e0e



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    p95: 200
  phases:
    - duration: 5
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    - path: "login_post_success.csv"
      skipHeader: true
      fields:
        - "client"
        - "email"
        - "otp"
scenarios:
  - name: "Scenario: Validate correct API behavior with valid payloads to login endpoint."
    flow:
      - post:
          url: "/login"
          headers:
            content-type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            client: "{{ client }}"
            email: "{{ email }}"
            otp: "{{ otp }}"
          expect:
            - statusCode: 201 
            - hasProperty: "token"
            - contentType: "application/json"
          capture:
            - json: "$.token"
              as: "token"
        think: 1
