# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=c5011f3c64

# Created Artillery YAML configuration
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "credentials_get_others.csv"
    fields:
      - "size"
      - "offset"
  plugins:
    expect: {}

scenarios:
  - name: "Testing Non-Successful API Requests"
    flow:
      - log: "Sending a request without authorization header"
      - get:
          url: "/credentials"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN_INVALID }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1

      - log: "Sending a request with unauthorized token"
      - get:
          url: "/credentials?size={{ size }}&offset={{ offset }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN_UNAUTHORIZED }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1

      - log: "Sending a request with invalid size parameter"
      - get:
          url: "/credentials?size={{ size_invalid }}&offset={{ offset }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1

      - log: "Sending a request with invalid offset parameter"
      - get:
          url: "/credentials?size={{ size }}&offset={{ offset_invalid }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1
