# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /credentials_get for http method type GET in artillery framework

# RoostTestHash=c5011f3c64


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "credentials_get_others.csv"
    fields:
      - "Token"
    skipHeader: true

scenarios:
  - name: "Test case for non-successful API requests"
    flow:
      # Request with missing "Token" in the header
      - get:
          url: "/credentials"
          headers:
            Token: ""
          expect:
            - statusCode: 401
          think: 1

      # Request with invalid header "Token"
      - post:
          url: "/credentials/invalid_header_token"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 403
          think: 1

      # Request with non-existing credentials
      - get:
          url: "/credentials/non_existing"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
          think: 1

      # Request with invalid method "DELETE" for /credentials endpoint
      - delete:
          url: "/credentials"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
          think: 1

      # Request with improper payload
      - post:
          url: "/credentials"
          json:
            invalidKey: "invalidValue"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
          think: 1

      # Request to an invalid URL
      - get:
          url: "/invalid_endpoint"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
          think: 1

      # Request exceeding server capacity limitations
      - get:
          url: "/credentials?size=51"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 413
          think: 1
