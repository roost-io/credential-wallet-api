# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /credentials_get for http method type GET in artillery framework
# RoostTestHash=c5011f3c64



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "credentials_get_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
  environments: 
    test:
      $processEnvironment: 
        AUTH_TOKEN: "example"

scenarios:
  - name: "Non-Successful API Requests"
    flow:
      # Request with invalid authentication token
      - get: 
          url: "/credentials"
          headers:
            Token: "Invalid_Token"
          expect:
            - statusCode: 401 
            - hasProperty: "error"
          think: 1

      # Request with missing required headers
      - get: 
          url: "/credentials"
          headers: {}
          expect:
            - statusCode: 400 
            - hasProperty: "error"
          think: 1

      # Request with invalid URL
      - get: 
          url: "/wrong_url"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
          think: 1

      # Testing edge case with a invalid (over-size) size query parameter
      - get:
          url: "/credentials?size=100"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
          think: 1
