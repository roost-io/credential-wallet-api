# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /apikeys/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=4affc26394


config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./my_processor.js"
  payload:
    path: "apikeys_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "non-success API responses"
    comment: "This scenario covers various non-successful API requests"
    flow:
      # Sending a GET request without Authorization headers
      - comment: "GET /apikeys/{id} without Authorization headers"
        get:
          url: "/apikeys/{{ id }}"
          expect:
            - statusCode: 401
        think: 1

      # Sending a GET request with Invalid Authorization headers
      - comment: "GET /apikeys/{id} with Invalid Authorization"
        get:
          url: "/apikeys/{{ id }}"
          headers:
            Token: "InvalidToken"
          expect:
            - statusCode: 403
        think: 1

      # Sending a GET request with Valid Authorization but invalid id
      - comment: "GET /apikeys/{id} with Valid Authorization but invalid id"
        get:
          url: "/apikeys/invalid-id"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1

      # Sending a GET request with Valid Authorization but incorrect URL
      - comment: "GET /invalid-url with Valid Authorization"
        get:
          url: "/invalid-url"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1
