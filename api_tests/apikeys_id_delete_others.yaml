# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /apikeys/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=4affc26394



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 20
      arrivalRate: 10
  payloads:
    - path: "apikeys_id_delete_others.csv"
      fields: ["id"]
      skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Incorrect API usage tests - 4xx error cases"
    flow:
      - comment: "Attempt to retrieve API key with invalid ID"
        get:
          url: "/apikeys/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
        think: 1

      - comment: "Attempt to access resource with incorrect URL"
        get:
          url: "/nonexistent/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
        think: 1

      - comment: "Attempt to access resource without authorization"
        get:
          url: "/apikeys/{{ id }}"
        expect:
          - statusCode: 401
        think: 1

      - comment: "Attempt to access resource with invalid authorization"
        get:
          url: "/apikeys/{{ id }}"
          headers:
            Token: "invalid_token"
        expect:
          - statusCode: 401
        think: 1

      - comment: "Attempt to access resource with incorrect authorization field"
        get:
          url: "/apikeys/{{ id }}"
          headers:
            WrongTokenField: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 401
        think: 1
