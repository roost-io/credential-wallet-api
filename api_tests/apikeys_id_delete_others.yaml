# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=4affc26394

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "apikeys_id_delete_others.csv"
    fields:
      - id
  plugins:
    expect: {}

scenarios:
  - name: "Testing Non-Success API requests"
    flow:
      - log: "Attempting a GET with an invalid UUID"
        get:
          url: "/apikeys/{id}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
        think: 1

      - log: "Attempting a DELETE request without auth token"
        delete:
          url: "/apikeys/{{ id }}"
          headers:
            Token: ""
          expect:
            - statusCode: 401
        think: 1

      - log: "Attempting a GET request with non-existent UUID"
        get:
          url: "/apikeys/{id}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
        think: 1
