# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=1ce4bf6e3c


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "branches_id_delete_others.csv"
    fields: ["id"]
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Non-Success API Requests Scenario"
    flow:
      - log: "Test for invalid Authorization token"
        post:
          url: "/recipients/branches/{{ id }}"
          headers:
            Token: "Invalid_Token"
          expect:
            - statusCode: 401
        think: 1

      - log: "Test for missing Authorization token"
        post:
          url: "/recipients/branches/{{ id }}"
          expect:
            - statusCode: 401
        think: 1

      - log: "Test for invalid URL"
        post:
          url: "/wrong_url/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1

      - log: "Test for missing id in the path"
        post:
          url: "/recipients/branches/"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
        think: 1

      - log: "Test for malformed id in the path"
        post:
          url: "/recipients/branches/invalid_id"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
        think: 1
