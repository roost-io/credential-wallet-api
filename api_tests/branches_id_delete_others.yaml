# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=1ce4bf6e3c

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 1
  payload:
    path: "branches_id_delete_others.csv"
    fields:
      - "id"
  plugins:
    expect: {}
  environments:
    env:
      target: "{{ $processEnvironment.API_HOST }}"
      auth_token: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "API error scenario"
    flow:
      - log: "Requesting with a wrong API key"
        get:
          url: "/recipients/branches/{{ id }}"
          headers:
            Token: "wrong token"
            content-type: "application/json"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
        think: 1
      - log: "Requesting with a non-existing ID"
        get:
          url: "/recipients/branches/123"
          headers:
            Token: "{{ auth_token }}"
            content-type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
        think: 1
