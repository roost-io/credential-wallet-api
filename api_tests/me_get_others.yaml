# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=ae6b8ce5ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  environments:
    dev:
      target: "http://localhost:3000"
  plugins:
    expect: {}
  payload:
    path: "me_get_others.csv"
    fields:
      - "email"
      - "password"
  phases:
    - duration: 60
      arrivalRate: 10
    - duration: 120
      arrivalRate: 20
scenarios:
  - name: "Non-success API responses"
    flow:
      - log: "Sending request without authorization"
      - get:
          url: "/me"
          headers:
            api-version: "1.0.0"
        expect:
          - statusCode: 401
          - hasProperty: "error"
          - hasProperty: "description"
      - think: 5
      
      - log: "Sending request with incorrect Token"
      - get:
          url: "/me"
          headers:
            Token: "IncorrectToken"
            api-version: "1.0.0"
        expect:
          - statusCode: 401
          - hasProperty: "error"
          - hasProperty: "description"
      - think: 5

      - log: "Sending request with the correct Token but invalid payload"
      - post:
          url: "/me"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
            api-version: "1.0.0"
          json:
            email: "InvalidEmail"
            password: "password"
        expect:
          - statusCode: 400
          - hasProperty: "error"
          - hasProperty: "description"
          - hasProperty: "value"
          - hasProperty: "field"
          - hasProperty: "schema_field"
      - think: 5

      - log: "Accessing an invalid URL"
      - get:
          url: "/invalid"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
            api-version: "1.0.0"
        expect:
          - statusCode: 404
      - think: 5
