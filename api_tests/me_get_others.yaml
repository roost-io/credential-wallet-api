# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /me_get for http method type GET in artillery framework
# RoostTestHash=ae6b8ce5ab



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
    - duration: 60
      arrivalRate: 2
  plugins:
    expect: {}
  payload:
    fields: ["Token"]
    skipHeader: true
    path: "me_get_others.csv"
scenarios:
  - name: Test scenario for non-successful API endpoints
    flow:
    
    # Making a GET request to a non-existent endpoint
      - get:
          url: "/non_existent_endpoint"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
          think: 5
    
    # Making a GET request to '/me' without Token
      - get:
          url: "/me"
          headers:
            Token: ""
          expect:
            - statusCode: 401
          think: 5

    # Making a GET request to '/me' with incorrect Token
      - get:
          url: "/me"
          headers:
            Token: "incorrect_token"
          expect:
            - statusCode: 401
          think: 5

    # Making an unauthorized POST request to '/me'
      - post:
          url: "/me"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            invalid_field: "invalid_value"
          expect:
            - statusCode: 405
          think: 5
