# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /me_get for http method type GET in artillery framework

# RoostTestHash=ae6b8ce5ab


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "me_get_others.csv"
    fields: ["email", "settings", "name", "image"]
  plugins:
    expect: {}
  defaults:
    headers:
      content-Type: "application/json"
      Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: "Failed GET calls for non-valid users"
    flow:
      - get:
          url: "/me"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1
      - get:
          url: "/invalidendpoint"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1
      - get:
          url: "/me"
          beforeRequest: "addInvalidHeader"
          expect:
            - statusCode: 403
            - hasProperty: "denied"
      - think: 1
  - name: "Failed POST calls for non-valid user updates"
    flow:
      - post:
          url: "/me"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1
      - post:
          url: "/me"
          json:
            settings: "{{ settings }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1
  - name: "Failed PATCH calls for non-valid user updates"
    flow:
      - patch:
          url: "/me"
          json:
            name: "{{ name }}"
            settings: "{{ settings }}"
            image: "{{ image }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1
