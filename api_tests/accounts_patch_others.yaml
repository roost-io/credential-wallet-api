# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /accounts_patch for http method type PATCH in artillery framework

# RoostTestHash=2758affb7e


config:
  target: "{{ $processEnvironment.API_HOST }}"
  environments:
    default:
      target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "accounts_patch_others.csv"
    fields:
      - "email"
    skipHeader: true
    order: sequence
  ensure:
    p95: 200
scenarios:
  - name: "Accounts endpoint scenarios with non success API responses"
    description: "Scenario testing Accounts endpoint for non successful responses"
    flow:
      - 
        # Invalid credentials
        post:
          url: "/login"
          json:
            email: "{{ email }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: ["message", "Invalid credentials"]
          think: 5
      -
        # Non-Existing account
        post:
          url: "/login"
          json:
            email: "{{ email }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: ["message", "Account not found"]
          think: 5
      -
        # Invalid URL
        get:
          url: "/invalidURL"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: ["message", "Invalid URL"]
          think: 5
