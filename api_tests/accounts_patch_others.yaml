# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /accounts_patch for http method type PATCH in artillery framework
# RoostTestHash=2758affb7e



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "accounts_patch_others.csv"
    fields:
      - "email"
    skipHeader: true
  environments:
    development:
      target: "http://localhost:3000"
      
scenarios:
  - name: "Test non-successful API requests"
    flow:
      - log: "Invalid email test case"
        patch:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "invalid_email"
          expect:
            - statusCode: 400
          think: 1
          
      - log: "Correct email but unauthorized test case"
        patch:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
            Token: "Invalid token"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 403
          think: 2

      - log: "Invalid URL test case"
        patch:
          url: "/invalid_url"
          headers:
            Content-Type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 404
          think: 1

      - log: "Valid email and authorized but server error simulated"
        patch:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          capture:
            header: "x-server-error"
            as: "serverError"
          expect:
            - statusCode: 500
          think: 2
