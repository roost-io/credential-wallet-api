# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /accounts_post for http method type POST in artillery framework

# RoostTestHash=53e96f9805


config:
  target: "{{ $processEnvironment.API_HOST }}"
  environments:
    default:
      target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "accounts_post_others.csv"
    fields: ["email"]
  plugins:
    expect: {}

scenarios:
  - name: "Failure Scenario: Non-Successful API Requests"
    flow:
      - log: 'Send POST request with missing required fields'
        post:
          url: "/accounts"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasProperty: message
          think: 1

      - log: 'Send POST request with invalid email field'
        post:
          url: "/accounts"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            email: "invalid mail"
          expect:
            - statusCode: 400
            - hasProperty: message
          think: 1

      - log: 'Send PUT request to update non-existed Account'
        put:
          url: "/accounts"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 404
            - hasProperty: message
          think: 1

      - log: 'Send GET request to non-existing Account'
        get:
          url: "/accounts"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: message
          think: 1

      - log: 'Send POST request with no authorization'
        post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 401
            - hasProperty: message
          think: 1

      - log: 'Send POST request to an incorrect URL'
        post:
          url: "/incorrect/url"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 404
            - hasProperty: message
          think: 1
