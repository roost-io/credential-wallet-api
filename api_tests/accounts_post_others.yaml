# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /accounts_post for http method type POST in artillery framework

# RoostTestHash=53e96f9805


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "accounts_post_others.csv"
    fields:
      - "email"
    skipHeader: true

scenarios:
  - name: "Negative Test Scenario for Account Creation"
    flow:
      - log: "Testing: POST request without the necessary body fields, expecting a 400 status."
      - post:
          url: "/accounts"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          expect:
            - statusCode: 400
          think: 1

      - log: "Testing: POST request with an invalid email, expecting a 400 status."
      - post:
          url: "/accounts"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            email: "invalid@email"
          expect:
            - statusCode: 400
          think: 1

      - log: "Testing: POST request with an unauthorized token, expecting a 403 status."
      - post:
          url: "/accounts"
          headers:
            Authorization: "Bearer invalidtoken"
            Content-Type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 403
          think: 1

      - log: "Testing: POST request to a non-existent endpoint, expecting a 404 status."
      - post:
          url: "/invalidendpoint"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 404
          think: 1
