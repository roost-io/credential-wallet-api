# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches/{id}/registrations_get for http method type GET in artillery framework

# RoostTestHash=49b35fa9c1


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20 
  plugins:
    expect: {}
  payload:
    path: "registrations_get_success.csv"
    fields:
      - "id"
      - "credentialUrl"
      - "size"
      - "offset"
  headers:
    Token: "{{$processEnvironment.AUTH_TOKEN}}"
    
scenarios:
  - name: "Recipient Registrations"
    flow:
      
      # First API request - Get Recipient Branch Registrations
      - get:
          url: "/recipients/branches/{{ id }}/registrations?credential-url={{ credentialUrl }}&size={{ size }}&offset={{ offset }}"
          expect: 
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "registrations"
            - hasHeader: "api-version"
            - hasHeader: "page-size"
            - hasHeader: "page-offset"
      - think: 5
