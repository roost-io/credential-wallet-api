# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /recipients/persons/{id}/registrations_get for http method type GET in artillery framework
# RoostTestHash=318c363c90



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    - expect
  payload:
    path: "registrations_get_success.csv"
    fields:
      - id
      - credentialUrl
      - size
      - offset
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 1
scenarios:
  - name: Validate Non-Success API Responses
    flow:
      - get:
          url: "/recipients/persons/{{ id }}/registrations?credential-url={{ credentialUrl}}&size={{ size }}&offset={{ offset }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: { "api-version": "1.0.0" }
            - hasProperty: ["registrations", "total"]
            - hasProperty: ["registrations", {"items": { "id": {}, "reference": {}, "startDate": {}, "endDate": {}, "status": {}, "credentialUrl": {}, "suspendedUntil": {}, "issuedAt": {}, "createdAt": {}, "updatedAt": {}}}]
        think: 5
