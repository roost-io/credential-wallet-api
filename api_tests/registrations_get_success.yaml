# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches/{id}/registrations_get for http method type GET in artillery framework

# RoostTestHash=49b35fa9c1


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 2
  payload:
    path: "./registrations_get_success.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      variables:
        apiKey: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "sample scenario"
    flow:
      - get:
          url: "/recipients/branches/{{id}}/registrations?id={{ id }}"
          headers:
            Token: "{{ apiKey }}"
          expect:
            - hasProperty: "total"
            - hasProperty: "registrations"
            - statusCode: 200
          afterRequest: "myProcessor"
      - think: 1
