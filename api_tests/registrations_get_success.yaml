# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /registrations_get for http method type GET in artillery framework

# RoostTestHash=5a542f8139


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "registrations_get_success.csv"
    fields: ["size", "recipient-id", "credential-url"]
    skipHeader: true
  defaults:
    headers:
      Token: "{{$processEnvironment.AUTH_TOKEN}}"
scenarios:
  - name: "Artillery Test Scenario for /registrations API endpoint"
    flow:
      # Get request on /registrations endpoint
      - get:
          url: "/registrations?size={{ size }}&recipient-id={{ recipient-id }}&credential-url={{ credential-url }}"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "registrations"
            - hasProperty: "registrations[0].id"
            - hasProperty: "registrations[0].reference"
            - hasProperty: "registrations[0].startDate"
            - hasProperty: "registrations[0].status"
            - hasProperty: "registrations[0].credentialUrl"
            - hasProperty: "registrations[0].createdAt"
            - hasHeader: "page-size"
            - hasHeader: "page-offset"
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
          beforeRequest:
            think: 1
