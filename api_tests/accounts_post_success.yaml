# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /accounts_post for http method type POST in artillery framework

# RoostTestHash=53e96f9805


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 2
  payload:
    path: "accounts_post_success.csv"
    fields:
      - "email"
    skipHeader: true
  plugins:
    expect: {}
  defaults:
    headers:
      Token: "{{$processEnvironment.AUTH_TOKEN}}"
scenarios:
  - name: Account creation scenario
    flow:
      - post:
          url: "/accounts"
          headers:
            commit-hash: "7a13sgd"
            Content-Type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 201 # It is expected that the account creation will be successful
            - contentType: "application/json"
            - hasHeader: "api-version" # API version is expected in the response header
            - hasHeader: "commit-hash" # The commit hash of the application is expected in the response header
          think: 5
