# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /accounts_post for http method type POST in artillery framework

# RoostTestHash=53e96f9805


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "accounts_post_success.csv"
    skipHeader: true
    fields:
      - "email"
  plugins:
    expect: {}

scenarios:
  - name: "Create a new account and validate responses"
    flow:
      - post:
          url: "/accounts"
          json:
            email: "{{ email }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
            commit-hash: "abc1234"
          expect:
            - statusCode: 201
            - hasHeader: "api-version"
            - hasHeader: "commit-hash"

      - think: 3

      - get:
          url: "/accounts"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "email"
    
      - think: 3
