# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=63322ffb26

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
      name: "Non-Success API Response Tests"
  plugins:
    expect: {}
  payload:
    path: "branches_put_others.csv"
    fields: ["cocNumber", "cocBranchNumber", "city", "tradeName", "phone", "email"]
    skipHeader: true
  environments:
    staging:
      target: "{{ $processEnvironment.API_HOST }}"

scenarios:
  - name: "Testing Various Non-Successful API Requests"
    flow:
      - log: "Sending invalid authorization token"
        post:
          url: "/recipients/branches"
          auth:
            bearer: "{{$processEnvironment.INVALID_AUTH_TOKEN}}"
          expect:
            - statusCode: 401
            - hasHeader: "api-version"
            - headerEquals: ["api-version", "1.0.0"]
            - hasProperty: "error"
            - hasProperty: "description"
          think: 2

      - log: "Sending invalid payload"
        post:
          url: "/recipients/branches"
          auth:
            bearer: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ invalidEmail }}" # Invalid value
          expect:
            - statusCode: 400
            - hasHeader: "api-version"
            - headerEquals: ["api-version", "1.0.0"]
            - hasProperty: "error"
            - hasProperty: "description"
            - hasProperty: "value"
            - hasProperty: "field"
            - hasProperty: "schema_field"
          think: 2

      - log: "Trying to access nonexistent branch"
        get:
          url: "/recipients/branches/nonexistent_branch"
          auth:
            bearer: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasHeader: "api-version"
            - headerEquals: ["api-version", "1.0.0"]
          think: 2

      - log: "Trying to delete nonexistent branch"
        delete:
          url: "/recipients/branches/nonexistent_branch"
          auth:
            bearer: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasHeader: "api-version"
            - headerEquals: ["api-version", "1.0.0"]
          think: 2
