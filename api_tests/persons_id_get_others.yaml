# ********RoostGPT********
# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /recipients/persons/{id}_get for http method type GET in artillery framework
# RoostTestHash=131d25110f



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "persons_id_get_others.csv"
    skipHeader: true
    fields:
      - "id"

scenarios:
  - name: "Testing non-successful API responses"
    flow:
      - post:
        # Incorrect payload. Expecting status code 400
        url: "/recipients/persons/{{ id }}"
        headers:
          Token: "{{ $processEnvironment.AUTH_TOKEN }}"
        json:
          garbage: "incorrect"
        expect:
          - statusCode: 400
        think: 1

      - post:
        # Empty payload. Expecting status code 400
        url: "/recipients/persons/{{ id }}"
        headers:
          Token: "{{ $processEnvironment.AUTH_TOKEN }}"
        json: {}
        expect:
          - statusCode: 400
        think: 1
 
      - get:
        # Valid request but expecting unauthorized response as not passing authorization token in header. Expecting status code 401
        url: "/recipients/persons/{{ id }}"
        expect:
          - statusCode: 401
        think: 1

      - get:
        # Incorrect URL. Expecting status code 404
        url: "/incorrect/url/{{ id }}"
        headers:
          Token: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
        think: 1

      - get:
        # Incorrect id. Expecting status code 400
        url: "/recipients/persons/incorrectId"
        headers:
          Token: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
        think: 1
