# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/persons/{id}_get for http method type GET in artillery framework

# RoostTestHash=131d25110f


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "persons_id_get_others.csv"
    fields:
      - "id"
  processor: "./my_processor.js"
  http:
    pool: 10
  ensure:
    p99: 200
scenarios:
  - name: "Negative API testing for /recipients/persons/{id} endpoint"
    flow:
      # Unauthorized request
      - get:
          url: "/recipients/persons/{{ id }}"
          headers:
            Token: "wrong_token"
          capture:
            - header: "www-authenticate"
              as: "authenticate"
          expect:
            - statusCode: 401
            - hasHeader: "www-authenticate"
          think: 5

      # Not found
      - get:
          url: "/recipients/persons/invalid-id"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
          think: 5

      # Method not allowed
      - put:
          url: "/recipients/persons/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
          think: 5

      # Unsupported Media Type
      - post:
          url: "/recipients/persons"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "text/plain"
          expect:
            - statusCode: 415
          think: 5
