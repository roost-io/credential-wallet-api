# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=131d25110f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "persons_id_get_others.csv"
    fields:
      - id
    skipHeader: true

scenarios:
  - name: "Invalid API usage simulation"
    flow:
      - log: "Running Person ID API with correct token but invalid UUID"
        post:
          url: "/recipients/persons/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: 
                description: "Descriptive Error Message"
                error: "validation/email-pattern"
      - think: 1
      
      - log: "Running Person ID API with invalid token and valid UUID"
        post:
          url: "/recipients/persons/{{ id }}"
          headers:
            Token: "invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - equals: 
                description: "Descriptive Error Message"
                error: "validation/email-pattern"
      - think: 1
      
      - log: "Running Person ID API with valid token and valid UUID but invalid URL"
        post:
          url: "/invalid_url/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
      
      - log: "Running Person ID API without Auth Token"
        post:
          url: "/recipients/persons/{{ id }}"
          expect:
            - statusCode: 401
      - think: 1
