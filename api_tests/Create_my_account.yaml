# Test generated by RoostGPT for test credential-integration-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "Create_my_account.csv"
    fields:
      - "accounts_body"
    skipHeader: true
  plugins:
    expect: {}
  variables:
    AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Create my account"
    flow:
      - post:
          url: "/accounts"
          headers:
            Authorization: "Bearer {{ AUTH_TOKEN }}"
          json:
            {{ accounts_body }}
          log: "Creating a new account with the specified information."
          expect:
            - statusCode: 201
            - hasProperty: "id"
            - hasProperty: "email"
      - think: 1
      - get:
          url: "/me"
          headers:
            Authorization: "Bearer {{ AUTH_TOKEN }}"
          log: "Verifying the account created using GET request for '/me'."
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "email"
