# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /logout_post for http method type POST in artillery framework

# RoostTestHash=64d33906e5


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
   path: "logout_post_success.csv"
   skipHeader: true
   fields:
     - exampleField1
     - exampleField2 

scenarios:
  - name: "Logout API test scenario"
    flow:
      - post: 
          url: "/logout?paramName={{ exampleField1 }}"
          # Here we set the headers for API request
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          # Here we define the test payload for API request
          json:
            exampleField1: "{{ exampleField1 }}"
            exampleField2: "{{ exampleField2 }}"
          # Here we define our expectations for the API response
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: { "api-version": "1.0.0" }
        # Think time after each request
        think: 5
