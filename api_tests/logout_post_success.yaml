# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /logout_post for http method type POST in artillery framework

# RoostTestHash=64d33906e5


config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./my_functions.js"
  ensure:
    p95: 200
  phases:
    - duration: 60
      arrivalRate: 5
      name: "Warm up the application"
    - duration: 120
      arrivalRate: 5
      rampTo: 50
      name: "Ramp up load to peak"
    - duration: 600
      arrivalRate: 50
      name: "Sustained load"
  payload:
    path: "logout_post_success.csv"
    fields:
      - "username"
      - "password"
  plugins:
    expect: {}
scenarios:
  - name: "User successfully logs out"
    flow:
      # Step 1 - User logs out
      - log: "Service request to log out"
      - post:
          url: "/logout"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: 
                "api-version": "1.0.0"
      - think: 1
