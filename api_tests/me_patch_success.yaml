# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /me_patch for http method type PATCH in artillery framework

# RoostTestHash=6dd436357d


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "me_patch_success.csv"
    fields:
      - "name"
      - "image"
      - "settings"
  plugins:
    expect: {}
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      headers:
        Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        Token: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: Change my settings or account name
    flow:
      - log: "Given: I am a logged in user"
      - patch:
          url: "/me"
          headers:
            content-type: "application/json"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - hasProperty: "id"
            - hasProperty: "did"
            - hasProperty: "name"
            - hasProperty: "image"
            - hasProperty: "email"
            - hasProperty: "settings"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
            - statusCode: 200
            - equals: 
                - "name"
                - "{{ name }}"
            - matchesRegexp: 
                - "image"
                - "{{ image }}"
            - hasHeader: "api-version"
            - headerEquals: ["api-version", "1.0.0"]
      - think: 1
      - log: "After: I have changed my settings or account name"
