# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /me_patch for http method type PATCH in artillery framework

# RoostTestHash=6dd436357d


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "me_patch_success.csv"
    fields: ["name", "image", "settings"]
    order: sequence
    skipHeader: true
  defaults:
    headers:
      Token: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Testing /me PATCH request"
    flow:
      - post:
          url: "/me"
          headers:
            Content-Type: "application/json"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          # Successfully updating the user profile
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "did"
            - hasProperty: "name"
            - hasProperty: "email"
            - hasProperty: "settings"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
      - think: 1
