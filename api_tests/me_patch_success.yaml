# Test generated by RoostGPT for test credential-wallet-rest-assured using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /me_patch for http method type PATCH in artillery framework

# RoostTestHash=6dd436357d


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "me_patch_success.csv"
    fields:
      - "name"
      - "settings"
  plugins:
    expect: {}

scenarios:
  - name: "Test API endpoints sequence with various valid requests"
    flow:
      - log: "Starting scenarios"
      
      # Test an update request
      - comment: "PATCH /me – Update user settings"
        patch:
          url: "/me"
          json:
            name: "{{ name }}"
            settings: "{{ settings }}"
          headers:
            Authorization: "Token {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "updatedAt"
            - hasProperty: "createdAt"
            - hasHeader: "api-version"
            - equals:
                - "{{ name }}"
                - "{{ settings }}"
        think: 1

      # Testing a get request
      - comment: "GET /me – Retrieve user settings"
        get: 
          url: "/me"
          headers:
            Authorization: "Token {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "updatedAt"
            - hasProperty: "createdAt"
            - hasHeader: "api-version"
        think: 1
